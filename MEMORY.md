# Chef & Serve — MEMORY.md

> Single source of truth for "what's in this codebase right now". Updated after every PR ships.
> If you're an AI agent or a new dev opening this repo for the first time, **read this first**.

## ▶ Resuming in a fresh chat / new dev?

Read in this order: **CLAUDE.md → MEMORY.md (this file) → WORKFLOW.md**, then
`docs/ai/` before any AI work. The active plan is at
`~/.claude/plans/goofy-moseying-truffle.md`. Everything shipped is in the **PR
ledger** below; anything planned-but-not-built is flagged ⏳ or noted under
"Open questions". You can pick up mid-phase from here with zero prior context —
each PR shipped with a migration, a `scripts/smoke-*.mjs`, and WORKFLOW.md
linkage. The hotel (klant) phase is **fully shipped** (PR-KLANT-0…5 + DOCS).

**Last updated:** **Chef shift-command portal (PR-1 + PR-2) — SHIPPED TO PROD (PRs #200 · #204 · #207 · #208 · #210; migration 0058 on `ep-icy-scene` BEFORE deploy; type-check+lint+build green; `smoke-chef-pr2-claim.mjs` 7/7; each prod deploy Ready).** The chef spine — Available → Offered → Accept → Briefed → Clock-in/out → Paid — is now built behind flags: smart open-shift cards (fit% reuses the matching scorer, road-distance, urgency, "waarom", gross+hours+perks, vraag-Maarten), Availability 2.0 (one-tap niet-beschikbaar + herhaal-vorige-week + preferred-work form), emergency **first-qualified-wins claim** (race-safe atomic headcount+shift-status guarded INSERT; honours the chef's `availableForEmergency` consent), offer **lifecycle** (`seen_at`/`expires_at`, status chip, losing-chef "vervuld" message, 6-hourly expiry-sweep worker `workers/offer-expiry.ts` → `/api/cron/offer-expiry`), and **soft flag-gated matching** of `travelRadiusKm`+`avoidPreferences` (`prefsAdjust` in `matching.ts`). New `chefs` cols (travelRadiusKm/availableForEmergency/avoidPreferences/minStartHour/availabilityNotes) + `placements.seen_at/expires_at` + `shifts.is_emergency` (0058). All dark — flags default OFF: `CHEF_OPEN_SHIFTS_ENABLED` · `EMERGENCY_CLAIM_ENABLED` · `OFFER_EXPIRY_SWEEP_ENABLED` · `MATCHING_PREFS_ENABLED`. Roadmap: `~/.claude/plans/yes-chef-side-needs-vast-toast.md`. Deferred: replacement-handover (admin-lane), richer favourites, PR-3 (shift brief + Arrival Trust PWA). Prior — Ops 4-area ship — Facturatie (`invoices`/`invoice_lines`, generate→send→pay→void, klant + admin UI) · owner Reporting (`/admin/business/reporting` time-series) · klant `/client/week` grid · planbord rejection surface — SHIPPED TO PROD (commit `27707b4` · migrations 0040/0041 on `ep-icy-scene` BEFORE deploy · type-check+lint+build green · smokes invoicing 29 / reporting 15 / klant-week 6 / planbord 25 · email render 29/29).** Prior — **AI-capabilities sprint — owner assistant now 48 tools + parallel tool-calls + a runnable eval harness** — **SHIPPED TO PROD (PR #81–#85, NO migration).** (1) **Bigger brain, efficiënter (#82):** the brain now **batches independent tools into one round-trip** and the agent loop runs them concurrently (`Promise.all` in `src/lib/ai/runtime/agent.ts`), pausing on the confirm-gate for any *action* in a mixed batch — verified live (gpt-5.4 pulled `business.overview`+`roster.overview` in one turn). Back-compatible with scripted single-`tool_call` brains; `openai-brain.ts` emits/parses the full `tool_calls` array. (2) **+6 oversight read tools** exposing the Restricted, tool-only data RAG never indexes: `audit.search` · `documents.list_for_chef` · `documents.expiring` (all chefs, soonest-first) · `privacy.list_requests` · `email.status` (delivery/bounce, **no bodies**) · `payroll.read` (batches + marge). All read-only, **permission-gated** (audit/chefs/privacy/emails/payroll `.read`), **metadata/aggregate-only** — the read-models (`src/lib/ai/read-model/oversight.ts`) strip bytes/payloads/raw text. (3) **Runnable eval harness** `scripts/eval-ai.mts` (#84): golden routing + safety-refusal against the **real brain** (planning-only, ~15 calls, no execution/DB writes) — **15/15** live. The release regression net: prompt-injection · "negeer de veiligheidsregels" · mass-send · consent-on-behalf · exported-row-mutation all **refuse in words**; "keur alle uren goed" + "annuleer Daniels dienst" **read first**. The eval *surfaced* the `documents.expiring` gap (G5 had no global route) and then *confirmed* the fix (#85). Census **48 tools (29 read / 13 act / 6 personal)**. Gate: type-check 0 · `smoke-ai-tools` 184/0 · `smoke-ai-safety` 68/0 · `eval-ai` 15/15 · `smoke-ai-spine` 36/0 · `smoke-ai-brain` 28/0 · lint+build green. Docs: `docs/ai/ai-evaluation-set.md` marks the harness live; `WORKFLOW.md` §1.22 + §7.3 updated. ⚠ **OpenAI key rotation STILL pending (owner)** — prod `*.semantic_search`/`knowledge.search` depend on it; Railway `OPENAI_API_KEY` + DB-branch confirm still open. ⚠ WhatsApp (sent.dm) sender built but **awaits Meta template approval** before wiring triggers + a `whatsapp.send` tool; the email templates (`src/emails/*`) are owned by **another chat — DO NOT TOUCH**.

**Prior — Chunked + PII-redacted notes-RAG (Stage 2)** — **SHIPPED TO PROD** (manual migration `drizzle/manual_ai_embeddings.sql` applied via `scripts/apply-ai-embeddings.mts`: new **`ai_embeddings`** table — `vector(1536)` + HNSW cosine index + soft-supersede). The owner assistant gained a **`knowledge.search`** tool (census now **38 tools — 22 read / 10 act / 6 personal**): semantic recall over the free-text NOTES corpus (chef/klant-notities, dienstomschrijvingen, contactlogs) with **human citations** ("Notitie over chef Lisa de Vries") — complements the per-row `*.semantic_search`. Pipeline (`src/lib/ai/rag/*`): build text → **redact PII** (the Stage-1 tested `redact()` — email/phone/IBAN/BSN/card/DOB) → density-gate (>30% redacted ⇒ skip) → **chunk** (~500-tok, paragraph-aware) → embed (`text-embedding-3-small`) → **soft-supersede + insert**, idempotent via content hash (`scripts/rag-ingest.mts`). **Access control is load-bearing + PURE** (`src/lib/ai/rag/access.ts`, unit-tested key-free in `smoke-ai-rag`): retrieval filters by **tenant_scope** ∩ caller-scopes AND **visibility** BEFORE the LLM sees a chunk — owner spans all tenants, chef/klant scoped to self + placement-bridge (so the future chef/klant PAs are safe-by-construction). Ingested LIVE: **45 chunks** (9 chef-notes + 9 chef-profielen + 5 klant-notes + 22 diensten; contactlogs none yet). Verified: `smoke-ai-rag` **33/0** (incl. the access-filter logic) + LIVE `scripts/smoke-ai-rag-retrieval.mts` **9/0** (NEVER-sources allowlist · redaction-in-corpus · **chef-A-never-sees-chef-B** · admin_only invisible to a chef). The **nightly auto-refresh is now wired app-side** (`GET /api/cron/rag-ingest` → `ingestAll()`, `vercel.json` cron `0 3 * * *`, gated on `CRON_SECRET`) — NOT a Railway worker, because the standalone worker deploy can't import the shared redact/chunk/sources/ingest and copying them would risk redaction drift; the app runtime has `ingestAll()` natively. **Retention + AVG purge done too:** `workers/retention.ts` prunes superseded chunks >30d + chunks of sources soft-deleted >30d (double-gated), and `eraseUserData` synchronously purges an erased subject's chunks via `src/lib/ai/rag/purge.ts` (`smoke-avg-erasure` 30/0 still green). OpenAI key rotation still pending (owner). **`CRON_SECRET` set on Vercel (prod+dev) + prod redeployed; live cron returns HTTP 200.** ⚠⚠ **DB TOPOLOGY GOTCHA (read before any prod DB work):** `.env.local` points at a Neon **DEV** branch (`ep-green-mouse-…`); **Vercel prod uses a DIFFERENT branch (`ep-icy-scene-…`)**. So every "live" verification via `.env.local` (this session AND the prior per-row-embedding run) actually hit DEV. The real prod DB (`ep-icy-scene`, fresher data) has now been seeded explicitly: `ai_embeddings` table + 45 chunks ingested, AND per-row embeddings populated (11 chefs + 6 clients) so BOTH `knowledge.search` and `*.semantic_search` work for real users. To run a prod migration/ingest, pull the prod URL (`vercel env pull` → use its `DATABASE_URL[_UNPOOLED]`), don't rely on `.env.local`. **Open:** Railway workers — prod per-row embeddings were empty until now, so the Railway `embedding-refresh` worker isn't populating prod (most likely OBSERVE mode = no valid `OPENAI_API_KEY` on Railway, consistent with the pending key rotation; also confirm Railway's `DATABASE_URL_UNPOOLED` = `ep-icy-scene`, not the dev branch — if it's on dev, ALL prod workers are writing to the wrong DB). **Prior — AI assistant expansion + UX polish** — GitHub **PR #49, #51–#57** (2026-06-08) — **SHIPPED TO PROD (merged to main, NO migration)**. The owner AI assistant grew **21 → 34 tools** (18 read / 10 act / 6 personal): chef-360 reads (`chefs.work_summary`/`feedback`/`trends`), `roster.overview` (staffing picture), `clients.history` (klant-360), `planner.cockpit` (urgent queue), `shifts.suggest_chefs` (ranked matches with reasons), and **RAG semantic search** (`chefs`/`clients.semantic_search` over pgvector — the `embedding-refresh` worker was run LIVE: 11 chefs + 6 clients embedded); plus profile-change approve/reject + availability-reminder act tools. Also: a **conversation-context fix** (the system prompt now carries topic across turns), **gpt‑5.4 token+cost metering** on the `/admin/system` AI-tokens card (built-in $2.50/$15 per 1M, env-overridable `OPENAI_PRICE_*`), and **human Dutch enum labels everywhere** (no more `chef_de_partie` in the UI — all via `src/lib/labels.ts` `formatChefRole`/`formatShiftRole`/`formatSegment`). All confirm/permission-gated + smoke-covered (tools 136 · safety 48). Model `gpt-5.4`; OpenAI key rotation still pending (owner will do it). NEXT big piece: deeper **chunked + PII-redacted notes-RAG** per `docs/ai/rag-ingestion-contract.md` (needs a migration). **Prior — Chef workflow UX (PR #47/#48, NO migration):** **#47 login-trap fix:** one-click **"Uitnodigen & activeren"** on chef + client detail pages chains the tested `inviteChef/ClientToPortal` → `activatePortalUser` (account + `active` + welcome-mail in one step), killing the `invited`-limbo state where an account silently couldn't log in (login = passwordless magic-link, only sent to an `active` user; invite-only, no self-signup). **#48 Chef 360 rework** around *"kan deze chef de vloer op?"*: new pure `src/lib/domain/chef-inzetbaarheid.ts` `computeChefInzetbaarheid()` verdict (`ready`/`almost`/`blocked` + blockers/warnings · 31/31 smoke `scripts/smoke-chef-inzetbaarheid.mts`), a top-of-page **Inzetbaarheidskaart** (`chefs/[id]/_components/InzetbaarheidCard.tsx` — verdict + blocker chips + reliability strapline + consolidated actions invite&activate/mail/bel/↓bewerken), page reordered (verdict → Chef 360 → beoordeling → documenten → bewerken `#anchor` → AVG → portaal), and Chef 360's onboarding/profiel checklists + recente-diensten collapsed behind `<details>`. ⚠ Both built **blind from a `/tmp` GitHub clone** (this Mac's `~/Documents` is macOS-TCC/Full-Disk-Access-blocked → no local render/DB; gate = type-check+lint+smoke, Vercel builds on merge) — **verify card render + invite→login live**. Follow-up offered: **Klant 360** ("goede klant?" verdict). **Owner AI assistant (PA V1)** is also live (`src/lib/ai/**` · `/api/ai/chat` · dashboard chat widget · 21 tools · owner-gated + rate-limited) — ⚠⚠ **its OpenAI test key STILL NEEDS ROTATION** (was exposed in chat); `AI_CONFIRM_SECRET` is set; WhatsApp channel ON HOLD. **Prior:** Admin settings + per-person RBAC (PR-SET-1 + PR-RBAC, 2026-06) — **SHIPPED TO PROD**. PR-SET-1: owner cockpit de-leaked (no system/error info); the hours-reminders worker is now UI-toggleable via `/admin/business/instellingen` (new `business_settings` table; worker reads the flag via raw SQL, env stays as a kill-switch). PR-RBAC: the dormant permission layer is now **LIVE** — a permission catalog (`src/lib/rbac/catalog.ts`, system/business classes), a `user_permissions` override table + memoized effective-set engine (role ∪ grant − revoke), **all 113 admin gates flipped from role-name to `requirePermission`** (behavior-neutral, parity-proven), an editable role editor (`/admin/system/roles`), a per-user override editor, and an owner **Team page** (`/admin/business/team`) to create staff + set per-person capabilities — all behind escalation guards (`src/lib/rbac/guards.ts`: owners can't grant system perms or escalate). Migrations 0037 (`business_settings`) + 0038 (`user_permissions`) on prod; prod `role_permissions` reconciled to the catalog (owner=27, planner=14, super_admin=50, 0 system on owner/planner). Prior: PR-AUDIT. See "PR ledger".
**Live URL:** https://chefandserve2.vercel.app
**Repo:** github.com/Jezza5153/chefandserve2

---

## Map: where to find what

| Topic | File |
|---|---|
| Codebase guide (auto-loaded keystone) | `CLAUDE.md` — stack · hard rules · map · how-to-work |
| Plan (current operating plan) | `~/.claude/plans/goofy-moseying-truffle.md` |
| Strategic AI architecture | `AI_INTEGRATION.md` (4-layer model · data inventory · phased rollout) |
| Phase-by-phase tactical brief | `BUILD_PLAN.md` |
| 12-week roadmap | `ROADMAP.md` |
| State ledger (THIS FILE) | `MEMORY.md` |
| Process map / API linkage | `WORKFLOW.md` |
| AI playbooks + contracts | `docs/ai/` (see PR-AI-0) |
| DB schema (Drizzle) | `src/lib/db/schema.ts` |
| DB migrations | `drizzle/*.sql` |
| Smoke tests | `scripts/smoke-*.{sh,mjs}` |
| Backup scripts | `scripts/backup-*.sh` + `scripts/launchd/*.plist` |

---

## Stack

- **Frontend/Backend**: Next.js 15 App Router · React 19 · TypeScript
- **DB**: Neon Postgres (serverless) · Drizzle ORM · pgvector ready
- **Auth**: Auth.js v5 (JWT strategy) · Resend magic-link + Credentials (password+TOTP)
- **2FA**: OTPAuth · AES-256-GCM secret encryption · bcrypt recovery codes
- **Storage**: Cloudflare R2 (`chefandserve` bucket — scoped token)
- **Email**: Resend
- **Anti-bot**: Cloudflare Turnstile
- **Hosting**: Vercel (web) · Railway (workers)
- **Locale**: Dutch (UI) · `nl-NL` date formatting

---

## Currently-shipped flow at a glance

```
┌──────────────────────────────────────────────────────────────────┐
│ Marketing (chefandserve2.vercel.app)                              │
│  • Homepage, 17 service pages, about, work-with-us, contact       │
│  • /aanmelden → chef or klant Jotform                             │
│  • Header: [Inloggen] [Aanmelden]                                 │
├──────────────────────────────────────────────────────────────────┤
│ Jotform intake                                                    │
│  • Chef form  → chef_submissions  (idempotent on external_id)     │
│  • Klant form → client_submissions                                │
│  • Webhook: /api/intake/{chef,client}                             │
├──────────────────────────────────────────────────────────────────┤
│ Admin (super_admin + owner)                                       │
│  • /admin/business — KPIs + actions                               │
│  • /admin/business/inbox — Jotform submissions triage             │
│  • /admin/business/chefs · clients · shifts · roster              │
│  • /admin/system/{users,roles,errors,audit,webhooks,emails,…}     │
│  • Convert submission → chef/client master record                 │
│  • Invite chef/client to portal (status invited → active + email) │
│  • Invite internal staff (PR-A)                                   │
│  • Reset another internal's 2FA (PR-C0)                           │
├──────────────────────────────────────────────────────────────────┤
│ Chef portal (mobile-first)                                        │
│  • /chef — "wat moet ik nu doen?" dashboard (Today/Action/Money)  │
│  • /chef/hours — list + /chef/hours/[id] simple-form              │
│  • /chef/shifts/[id] — accept/reject + cancel (severity-aware)    │
│    with contact card (tel:/WhatsApp/Maps)                          │
│  • /chef/availability calendar · /chef/profile editable           │
│  • /chef/notifications · /chef/calendar (ICS subscribe)           │
├──────────────────────────────────────────────────────────────────┤
│ Klant portal                                                      │
│  • /client — "actie nodig" dashboard                              │
│  • /client/shifts/[id]/hours — receipt-style sign/reject          │
│  • /client/request · /client/notifications · /client/calendar     │
├──────────────────────────────────────────────────────────────────┤
│ Auth                                                              │
│  • /login (magic-link primary, password+TOTP behind toggle)       │
│  • /login/forgot-password · /login/lost-2fa                       │
│  • /recover/password · /recover/2fa (purpose-bound tokens)        │
│  • /admin/account/setup/* (force-enrollment wizard)               │
│  • TOTP_ENFORCE=true (12h re-prompt per device)                   │
└──────────────────────────────────────────────────────────────────┘
```

---

## PR ledger

### Shipped (in production)

| PR | Description | Status |
|---|---|---|
| CHEF-PR1+2 (#200 · #204 · #207 · #208 · #210) | Chef shift-command portal: smart open-shift cards (fit%/travel/urgency/"waarom"/gross+perks/vraag-Maarten) · Availability 2.0 (quick-blocks/repeat-week/preferred-work) · emergency first-qualified-wins claim (atomic headcount+shift-status guard · `availableForEmergency` consent) · offer lifecycle (seen/expires chip · losing-chef msg · 6h expiry-sweep worker `workers/offer-expiry.ts`) · soft flag-gated matching of travelRadius/avoid prefs · richer decline reasons | ✅ SHIPPED (migration **0058** on `ep-icy-scene` BEFORE deploy · type-check+lint+build green · `smoke-chef-pr2-claim.mjs` 7/7 · workers tsc green · each prod deploy Ready). New cols: `chefs.{travelRadiusKm,availableForEmergency,avoidPreferences,minStartHour,availabilityNotes}` · `placements.{seen_at,expires_at}` · `shifts.is_emergency`. Flags **FLIPPED ON** in prod 2026-06-15: `CHEF_OPEN_SHIFTS_ENABLED` · `EMERGENCY_CLAIM_ENABLED` · `OFFER_EXPIRY_SWEEP_ENABLED` (+ Railway) · `MATCHING_PREFS_ENABLED` (+ `OFFER_EXPIRY_HOURS` def 24) |
| CHEF-PR0/3/4/5/6/8/9/10 (#214 · #215 · #216 · #217 · #219 · #221 · #222 · #223 · #224 · #225 · #227) | Chef-side roadmap completion (plan `~/.claude/plans/yes-chef-side-needs-vast-toast.md`): **PR-0** 5-tab nav + fix-my-profile blockers + Bel-Maarten · **PR-3a** structured shift brief + `clients.non_negotiables` (mig 0059) · **PR-3b** Arrival Trust PWA `shift_arrival_checks` (mig 0060, on-device 1km, no coords) · **PR-8** Money Explainer `/chef/money` (`src/lib/money.ts`) · **PR-4a** clock-out review `shift_hour_reviews` (mig **0062**) · **PR-4b** owner clock-out digest (`ai/read-model/clockout-signals.ts` + `/api/cron/clockout-digest` + worker) · **PR-9a** payout pipeline + vakantiegeld-schatting on `/chef/earnings` (`domain/chef-payments.ts`, no mig) · **PR-6** reliability-aware matching (`getReliabilityForChefs` + `reliabilityAdjust`) · **PR-10** planned-vs-actual + client-overpromise AI tools `clients.overpromise`+`reports.planned_vs_actual` (`ai/read-model/overpromise.ts`, eval-gated) · **PR-9b** vacation+expense request flows `chef_vacation_requests`+`chef_expense_claims` (mig **0063**) + `/chef/declaraties` + `/admin/business/chef-requests` · **PR-5** skill-tag taxonomy `chefs.skill_tags` (mig **0064**) + `domain/skill-tags.ts` vocab + `tagsAdjust` matching boost · **replacement-handover** (#229, R2#13) `domain/replacement-handover.ts` + `stopArrivalMonitoring` + `transitionPlacement` hook (accepted/confirmed→cancelled → "ga niet naar de locatie" push + stop Arrival Trust), flag `REPLACEMENT_HANDOVER_ENABLED` · **PR-7 docs slice** (#230) `/chef/documenten` view/download/upload/delete via existing `chefDocuments`+R2+`FileUploader` (no mig, no invoices-lane touch) | ✅ SHIPPED (each: type-check+lint+build green · per-PR `smoke-chef-*.mjs` · workers tsc green where touched · AI gates 350/135 + 68-case eval green for PR-10 · migs 0059/0060/0062/0063/0064 applied to `ep-icy-scene` before merge · prod deploy Ready). Flags default **OFF** (owner-gated): `ARRIVAL_TRUST_ENABLED` (needs AVG/DPIA) · `MONEY_EXPLAINER_ENABLED` (verify tables) · `CLOCKOUT_DIGEST_ENABLED` · `MATCHING_RELIABILITY_ENABLED` · `MATCHING_TAGS_ENABLED` · `REPLACEMENT_HANDOVER_ENABLED`. **Solo roadmap DONE + 2/3 coordinate-lane done. Deferred (genuine tech reasons):** PR-7 **invoice-generation** half (invoicing chat owns active `invoices` model on shared prod → schema-collision risk) · PR-11 NL/EN i18n (huge + `src/emails/` lane + sequence late on settled surface); follow-ups: PR-9b receipt-upload UI (`receipt_r2_key` ready) · client requirement-tag editor (reuse PR-5 vocab) |
| PR-Z | Wizard 2FA crash fix (Next.js 15 cookies rule) | ✅ live |
| PR-A | Internal staff invite UI + simplified /login | ✅ live |
| PR-C0 | Admin Reset 2FA + cookie revocation chain (v2 cookie format) | ✅ live |
| PR-B | TOTP_ENFORCE=true + TOTP_REVERIFY_HOURS=12 | ✅ live |
| PR-C | Recovery flows (forgot password + lost 2FA, Fence 5 purpose-bound tokens) | ✅ live (migration 0010 applied) |
| PR-D | UX polish (users-list 2FA/last-login columns, calendar today ring, client request success card) | ✅ live |
| PR-AUDIT | Backend-audit remediation: `client_submissions.client_id` FK (cross-tenant scoping) · klant emails via `recipientsForClient` · KPI bucketed by `confirmedAt` · chef-proposal + portal-invite email tracking · `deliver-outbox` + `hours-reminders` workers · CSP excluded from error KPI · dead-code/doc cleanup | ✅ SHIPPED (commit b0b92f0 · migration 0035 on prod · 17/17 prod smoke) |
| PR-SET-1 | Admin settings: owner cockpit de-leak + UI-toggleable hours-reminders (`business_settings` table) | ✅ SHIPPED (commit 86b0b3d · migration 0037 on prod · 17/17 prod smoke · 4/4 flag smoke) |
| PR-RBAC | Per-person permissions: catalog + `user_permissions` engine + 113-gate flip (role-name→permission, behavior-neutral) + role/override editors + owner Team page + escalation guards (G1–G5) | ✅ SHIPPED (commits c5ffd3d…087d2d9 · migration 0038 + prod seed-reconcile · parity 72/72 · guards 14/14 · manage e2e 5/5). Re-run `scripts/audit-permission-parity.ts` after any gate/catalog edit. Coordinate migration numbers with the teammate. |
| PA-V1 | Owner AI assistant: channel-agnostic agent loop + OpenAI brain (native tool-call threading) · 21 tools (8 read / 7 act / 6 personal) **[baseline — now 38 tools; see #49/#51–#57 + notes-RAG]** · owner-gated `/api/ai/chat` + dashboard chat widget · rate-limit `ai_chat_user` (30/min) · reminders + memory as jsonb bags in `business_settings` · smokes `smoke-ai-{spine,brain,tools,safety}` | ✅ live (no migration). ⚠ OpenAI **test key NEEDS ROTATION** (exposed in chat) · `AI_CONFIRM_SECRET` set · WhatsApp = **messaging channel** via sent.dm (`sendWhatsApp`, key set), NOT an AI channel — needs an approved template to send |
| #47 | Login-trap fix: one-click **"Uitnodigen & activeren"** (chains `inviteChef/ClientToPortal` → `activatePortalUser`) on chef + client detail pages — no more `invited`-limbo that can't log in | ✅ live (no migration · merged to main 2026-06-08) |
| #48 | Chef 360 rework ("kan deze chef de vloer op?"): pure `computeChefInzetbaarheid()` verdict + top **Inzetbaarheidskaart** (`chefs/[id]/_components/InzetbaarheidCard.tsx`) + page reorder + `<details>` overload collapse | ✅ live (no migration · smoke 31/31 · was stacked on #47). ⚠ built blind from /tmp clone — verify card render live |
| #49 | AI data-write tools + context fix + tokens + reminders: `chefs.{list,approve,reject}_profile_change` (shared `decideChefProfileChange` domain fn, reused by the admin page) · `chefs.send_availability_reminder` + `workers/availability-reminder.ts` (Thu 09:00, gated off via `business_settings['availability_reminders']`) · conversation-context fix (system prompt) · gpt-5.4 token+cost metering (`ai_usage` jsonb tally in business_settings + AI-tokens card) | ✅ live (no migration) |
| #51 | Human Dutch enum labels everywhere — every raw `vakniveau`/`roleNeeded`/`segment` render routed through `src/lib/labels.ts` (12 display sites + form dropdowns; deleted roster's dup `humanize`) + built-in gpt-5.4 pricing on the AI card | ✅ live (no migration) |
| #52–#57 | AI tools-first batch (21→34): `chefs.work_summary`/`feedback`/`trends` · `roster.overview` · `clients.history` · `planner.cockpit` · `shifts.suggest_chefs` · `chefs`/`clients.semantic_search` (RAG/pgvector). All confirm-free reads wrapping tested domain logic; `embedding-refresh` run LIVE to populate vectors | ✅ live (no migration) |
| #58–#60 | 2 small read tools → census 37: `shifts.margin` (omzet−loon per dienst) · `contacts.timeline` (laatste contact met chef/klant) | ✅ live (no migration) |
| #61 | Notes-RAG **Stage 1** (foundation): `src/lib/ai/rag/redact.ts` (PII redaction — email/phone/IBAN/BSN/card/DOB, every pattern unit-tested) + `chunk.ts` (~500-tok paragraph-aware) + `scripts/smoke-ai-rag.mts` | ✅ live (no migration · smoke 17/0) |
| Notes-RAG **Stage 2** | Chunked + PII-redacted notes-RAG end-to-end: **`ai_embeddings`** store (manual migration `manual_ai_embeddings.sql` — `vector(1536)` + HNSW cosine + soft-supersede) · ingestion `src/lib/ai/rag/{sources,ingest}.ts` + `scripts/rag-ingest.mts` (redact→density-gate→chunk→embed→supersede, idempotent via content hash) · **PURE** scope/visibility access filter `rag/access.ts` + cosine retrieval `rag/retrieve.ts` · owner-facing `read-model/knowledge.ts` (human citations) · **`knowledge.search`** tool (38th; 22 read/10 act/6 personal). Owner spans all tenants, chef/klant scoped to self+placement-bridge (future PAs safe-by-construction) | ✅ SHIPPED (migration `manual_ai_embeddings.sql` on prod · **45 chunks ingested LIVE** · `smoke-ai-rag` 33/0 incl. access-filter + LIVE `smoke-ai-rag-retrieval` 9/0: NEVER-allowlist · redaction-in-corpus · **chef-A-never-sees-chef-B** · admin_only invisible to chef) |
| Notes-RAG **autonomy** | Production-autonomous RAG: **nightly auto-refresh** app-side (`GET /api/cron/rag-ingest` → `ingestAll()`, `vercel.json` cron `0 3 * * *`, `CRON_SECRET`-gated — app-side because the standalone Railway worker can't import the shared redact/chunk pipeline) · **retention** (`workers/retention.ts` strategy 5: superseded chunks >30d + erased-source chunks >30d, double-gated) · **synchronous AVG purge** on erasure (`src/lib/ai/rag/purge.ts` ← `eraseUserData`) | ✅ SHIPPED (smoke-ai-rag 37/0 · LIVE smoke-ai-rag-retrieval 13/0 incl. purge round-trip + retention-SQL · retention dry-run clean · smoke-avg-erasure 30/0) |
| Notes-RAG **docs index** | `knowledge.search` now also covers **project documentation** (MEMORY/WORKFLOW/AI_INTEGRATION/README/CLAUDE + `docs/ai/*.md`): heading-aware markdown chunking (`chunkMarkdown`), `source_table='docs'`, `tenant_scope='internal'` + `visibility='admin_only'` (owner-only). Ingested by `scripts/rag-ingest.mts` (script-only — Vercel cron can't read repo files). 20 files → **523 chunks** (dev + prod) | ✅ SHIPPED (smoke-ai-rag 41/0 · retrieval smoke 13/0 with docs allowlisted · functional doc-hit confirmed). ⏳ CV-text OCR deferred (needs R2-fetch + pdf/OCR dep) |
| **Chef-assistent V1** | A SECOND assistant persona for chefs (read-only, own-data-only). New `AiActor.subject` ({kind,entityId}) — chef/klant tools key off the session-resolved subject, NEVER a model id ("auth IS the lookup"). `resolveChefActor` · chef read-model (`read-model/chef-self.ts`) · 4 tools `mijn.diensten`/`mijn.uren`/`mijn.beschikbaarheid`/`mijn.profiel` (`permission:null`+`risk:read`) · `buildChefRegistry` · `runChefAssistant` · `CHEF_SYSTEM_PROMPT` · `POST /api/ai/portal/chat` (gated on `session.user.kind`) · floating widget on the chef portal (reuses `AssistantChat`/`AssistantWidget` with an `endpoint` prop). | ✅ SHIPPED (smoke-ai-portal 14/0: read-only + can't-be-steered-to-another-chef + live scoped exec + clean no-subject error · owner gate unregressed · live agent answers a real chef with their own data). |
| **Klant 360 verdict** | "Goede klant?" — pure `computeClientHealth()` (`src/lib/domain/client-health.ts`: sterk/goed/aandacht + strengths/watchpoints over volume, marge, vaste chefs, openstaande handtekeningen, tekensnelheid, feedback) mirroring the Chef inzetbaarheid pattern. Reuses `getClientSummary` signals (no new queries) via `getClientHealth(clientId)`. Top-of-page **ClientHealthCard** on `/admin/business/clients/[id]` + owner AI tool **`clients.health`** (census 39 tools). | ✅ SHIPPED (smoke-client-health 11/0 pure · ai-tools 154/0 · ai-safety 53/0 · build green) |
| **Klant-assistent V1** | The klant mirror of the chef assistant — read-only, own-data-only. `resolveClientActor` · klant read-model (`read-model/client-self.ts`) · 4 tools `onze.diensten`/`onze.uren`/`onze.vaste_diensten`/`onze.aanvragen` · `buildClientRegistry` · `runClientAssistant` · `CLIENT_SYSTEM_PROMPT` · same `POST /api/ai/portal/chat` (branches on `session.user.kind`) · floating widget on the klant portal. | ✅ SHIPPED (smoke-ai-portal 28/0 — now covers BOTH personas: read-only + no-id-injection + live scoped exec + clean no-subject error · owner gate unregressed · type-check+lint+build green) |
| **Assistent-hardening** | (1) **Widget-gesprek blijft behouden** bij sluiten/navigeren — `AssistantChat` persisteert `msgs`+`pending` in `sessionStorage` per endpoint (owner/chef/klant gescheiden; skip-first-save guard tegen leeg-overschrijven) + **"Gesprek wissen"**-knop. Lost de bekende widget-unmount-reset op (geldt voor alle drie de assistenten). (2) **Portal tool-routing eval** `scripts/live-ai-portal-eval.mts` (chef+klant, planning-only, manueel — mirror van `live-ai-eval`). | ✅ SHIPPED (type-check+lint+build green · portal eval routet correct: chef 5/5, klant ✓) |
| **Portal round-out + placements-RAG** | (1) 4th tool per persona: chef `mijn.beschikbaarheid` (doorgegeven beschikbaarheid) · klant `onze.vaste_diensten` (terugkerende templates). (2) New RAG source **`placements.outcome`** (afgeronde plaatsingen + beoordeling, `admin_only` zodat alleen de owner-assistent ze ophaalt → ratings-internal-only blijft gehandhaafd) — "welke chef deed het goed bij deze klant". | ✅ SHIPPED (smoke-ai-portal 36/0 · retrieval smoke 13/0: placements allowlisted + **admin_only onzichtbaar voor chef** + chef-A-never-sees-chef-B · 25 placement-chunks ingested LIVE) |
| **CV-tekst-RAG** | `knowledge.search` indexeert nu ook **chef-CV's** — alleen door de chef zélf geüploade tekst-PDF's (`chef_documents` type=cv, `uploaded_by = chef.userId`; admin/derde-partij + ID-docs uitgesloten per contract). `ingestCvs` (`src/lib/ai/rag/ingest.ts`): R2-bytes (`getObjectBytes`) → `unpdf`-tekstextractie (géén OCR — scans/zonder ingebedde tekst worden overgeslagen) → redact → chunk → embed. `tenant_scope=chefId:<id>`, `visibility=chef_own_and_admin`. Voedt de matching-/rooster-vragen van Maarten met CV-inhoud. Dep: `unpdf@^1.6.2`. **Nightly cron-maintained** (CV-ingestie zit in `ingestAll` → de Vercel-cron pikt nieuwe CV's vanzelf op; `unpdf` dynamic-imported). | ✅ SHIPPED (type-check+lint+**build** groen incl. unpdf · pipeline live geverifieerd tegen prod: R2 configured, 0 chef-geüploade CV's nu → degradeert netjes; defensieve try/catch per doc kan ingestie nooit breken). ⏳ unpdf-extractie nog niet op een echte CV-PDF getest (geen PDF-docs in DB); indexeert vanzelf zodra een chef een CV-PDF uploadt |
| **WhatsApp send (sent.dm)** | `src/lib/whatsapp.ts` `sendWhatsApp({to:[E.164], template:{id/name,parameters}, channel, sandbox})` → `POST https://api.sent.dm/v3/messages`, auth `x-api-key` (`SENT_DM_API_KEY`, gezet op Vercel prod+dev + `.env.local`). Berichtenkanaal (reminders/notificaties), **GEEN AI-kanaal**. Injecteerbare transport → `scripts/smoke-whatsapp.mts` 15/0 (request-shape + success/error parsing, geen netwerk). Key live geverifieerd (sandbox-call → 200-auth, 400-validatie). | ✅ SHIPPED (sender + env + smoke + Vercel-key). ⏳ **Echt versturen vereist een vooraf goedgekeurde WhatsApp-template** (Meta-regel; sent.dm dwingt `template` af) — zodra Maarten een template aanmaakt + de naam deelt, wire ik die in de reminders (worker-side helper) + evt. een `whatsapp.send` AI-tool. Sleutel staat in chat → overweeg roteren. |
| **AI oversight tools** (blindspot sweep) | 3 read-tools die de assistent toegang geven tot **Restricted (tool-only) data** die RAG bewust nooit indexeert (`read-model/oversight.ts` + `tools/oversight.ts`, metadata-only): **`audit.search`** (auditlog: wie deed wat wanneer — `audit.read`, nooit de before/after-payloads) · **`documents.list_for_chef`** (chef-documenten: soort/status/verloop/geverifieerd — `chefs.read`, nooit bytes/URL) · **`privacy.list_requests`** (AVG-verzoeken: soort/status/termijn — `privacy.read`, nooit de ruwe tekst). Census **45 tools** (26 read / 13 act / 6 personal). | ✅ SHIPPED (smoke-ai-tools 175/0 · safety 65/0 · live geverifieerd tegen dev). Uit de blindspot-audit; rest buildable backlog (eval-harness, meer RAG-bronnen, worker-smokes) nog open. |
| **Parallel tool-calls** (#82) | "Bigger brain, efficiënter": de brain batcht onafhankelijke tools in één beurt; de agent-loop draait ze concurrent (`Promise.all`) en pauzeert op de confirm-gate zodra er een *actie* in de batch zit. `BrainStep` kreeg een `tool_calls`-variant; `openai-brain.ts` zendt/parset de volledige `tool_calls`-array; back-compat met scripted single-`tool_call`. | ✅ SHIPPED (smoke-ai-spine 36/0 + brain 28/0 incl. parallel-blokken · **live**: gpt-5.4 haalde `business.overview`+`roster.overview` in één beurt). |
| **Oversight II + eval + documents.expiring** (#83–#85) | (a) 3 read-tools erbij: **`email.status`** (aflever/bounce — `emails.read`, nooit de inhoud) · **`payroll.read`** (batches + marge — `payroll.read`) · **`documents.expiring`** (alle chefs, eerst-verlopend — `chefs.read`). (b) **Runnable eval-harness** `scripts/eval-ai.mts`: golden routing + safety-refusal tegen de echte brain (planning-only, ~15 calls) — **15/15**; release-regressienet (`docs/ai/ai-evaluation-set.md` markeert 'm live). De eval bracht de `documents.expiring`-gap aan het licht (G5) en bevestigde de fix. Census **48 tools** (29 read / 13 act / 6 personal). | ✅ SHIPPED (smoke-ai-tools 184/0 · safety 68/0 · eval-ai 15/15 · lint+build green). |
| **Playbook → Maarten's voorkeuren** (#87) | De 4 lege voorkeur-slots in `src/lib/ai/playbook.ts` ingevuld: standaard **kort & bondig** · **geen vaste bijzonderheden** (live via memory-tool) · extra voorzichtig bij plaatsing-annuleren + klant-mail. De CC-voorkeur ("altijd Maarten in CC") is **deterministisch op tool-niveau**: `sendEmail()` kreeg een optionele `cc`; `sendOwnerEmail` zet `env.MAARTEN_EMAIL` in CC (overslaan als hij de ontvanger is), de confirm-preview toont de CC-regel. | ✅ SHIPPED (type-check 0 · tools 184/0 · safety 68/0 · spine 36/0 · eval 15/15 · **Vercel prod ● Ready**). ⚠ Vercel **Preview** deploys falen structureel (geen env-vars op de Preview-env) — pre-existing, raakt elke PR; **Production** heeft de vars en is groen. ⚠ Lokale `npm run build` brak op de invoice-WIP (`src/lib/invoice-labels.ts` → ontbrekende `@/lib/db/schema-types`) van **een andere chat** in dezelfde working tree — niet gecommit, niet van mij; NIET aanraken. |
| **Proactieve dagstart** (#89) | Ochtendbriefing: **gisteren-recap** (gedraaide diensten, uren nog niet rond — "hotel moet tekenen"/"chef moet indienen"/"afgekeurd", nieuwe hotel-opmerkingen) **+ vandaag-forecast** (diensten + open plekken, uren-wachtrij, verlopende docs). Deterministische NL-tekst (geen LLM → cron heeft geen OpenAI-key nodig). `read-model/briefing.ts` `buildDailyBriefing(now)` · on-demand tool **`briefing.daily`** (tool #49, cockpit.read) · work-endpoint `GET /api/cron/daily-briefing` (CRON_SECRET, levert in-app melding + e-mail via bestaande `OwnerMessageEmail`, WhatsApp **gated** op template-approval, idempotent via `lastSentDate`) · **Railway-ticker** `workers/daily-briefing.ts` (uurlijks, leest `business_settings['daily_briefing'].hour` via raw SQL, POST't de endpoint op Maartens gekozen uur — DST-correct via node-cron Amsterdam) · UI op `/admin/business/instellingen` (aan/uit + uur + kanalen). **Default UIT** (Maarten zet 'm zelf aan; mailt alleen hem). Census **49 tools**. | ✅ SHIPPED (type-check 0 · tools 187/0 · safety 69/0 · **smoke-briefing 9/0 live** · eval 16/16 incl. G-brief→briefing.daily · workers tsc clean · lint green · **Vercel prod ● Ready**). ⏳ Railway moet `CRON_SECRET` + `NEXT_PUBLIC_APP_URL` hebben + de ticker draaien om écht te pushen (anders werkt alleen de on-demand tool). WhatsApp-kanaal wacht op Meta-template. |
| **Eval-net uitgebreid** (#90) | `scripts/eval-ai.mts` van 16 → **25 cases** (15 golden + 10 safety): golden dekt nu de hele toolset (planner.cockpit, clients.health, contacts.timeline, shifts.margin, chefs.trends, knowledge.search, briefing.daily) — entity-first routing telt als correct; safety dekt extra grenzen (R10 gemarkeerde uren niet blind goedkeuren, R14 AVG niet auto-afhandelen, R-restore geen restore-naar-prod). | ✅ SHIPPED (eval-ai **25/25** live tegen gpt-5.4 — geen routing/safety-regressie). Het release-regressienet voor alle 49 tools. |
| **Answer-hygiene eval + raw-enum fix** (#91) | Nieuw **full-execution** evalniveau `scripts/eval-ai-answers.mts`: draait de échte owner-loop (gpt-5.4 + de echte 49-tool registry + echte read-tools tegen dev) en grep't het FINALE antwoord op grenzen die de planning-eval niet ziet — **R6** (nooit een ruwe status-enum) + **R7** (geen Payingit-overclaim "uitbetaald" zonder hedge). **Ving een echte bug:** "noem de status erbij" lekte `client_signed` — bron was de **tool-omschrijving** zelf ("(status client_signed)"). Fix: ruwe enums uit de hours-tool-omschrijvingen gehaald + `listHoursAwaitingApproval` stempelt nu de NL-label via `humanStatus()` ("Door klant akkoord"). | ✅ SHIPPED (eval-ai-answers **6/6** na fix · tools 187/0 · safety 69/0 · lint green). Per docs/ai/ai-evaluation-set.md de execution-based laag (R6/R7). |
| **RAG-bron: placement_comments** (#92) | Nieuwe RAG-bron `placement_comments.thread` in `rag/sources.ts` — de multi-actor comment-threads bij een plaatsing (operationeel signaal: te laat, lof van hotel, zorgen). **`admin_only`** (comments gaan in-app via `listVisibleComments` ownership-checked → RAG mag geen achterdeur zijn; alleen de owner-assistent haalt ze op). System-comments overgeslagen (ruis). Voedt `knowledge.search` voor "wat speelde er bij klant X / welke signalen kregen we over chef Y". | ✅ SHIPPED (type-check 0 · smoke-ai-rag 41/0 · **retrieval-smoke 13/0**: placement_comments allowlisted + admin_only onzichtbaar voor chef · SQL live-valid: ingest draaide schoon, 0 rijen in dev = geen geseede comments → vult vanzelf in prod via de nachtelijke cron). |
| **Tool: hours.detail** (#93) | `hours.detail(hoursId)` (tool #50, hours.read) — één urenregel volledig: chef · klant · dienst · gepland vs werkelijk + pauze · omzet/loonkosten/marge · status (NL-label) · klant-akkoord-tijdstip, met de **markeringen PROMINENT** (rooster-afwijking > ±30 min, chef/klant-notities, ontbrekend tarief) + advies of 't schoon is. Spiegelt `isMagicApproveEligible`. Vult de gedocumenteerde golden-case **G2** ("kan ik deze uren goedkeuren?") + ondersteunt de R10-veiligheidsregel (nooit blind een gemarkeerde regel goedkeuren). `read-model/hours.ts` `hoursDetailForAi`. | ✅ SHIPPED (type-check 0 · tools 190/0 · safety 70/0 · eval 26/26 incl. G2 · SQL live-valid op dev). Census **50 tools** (31 read / 13 act / 6 personal). |
| **Tool: shifts.detail** (#94) | `shifts.detail(shiftId)` (tool #51, shifts.read) — de volledige stand van één dienst: klant · wanneer · rol · status · **bezetting** (ingevuld/nodig + open) · het **team** (elke chef met plaatsingsstatus + uren-status) · aantal opmerkingen. Alle statussen als **NL-labels** (eigen maps in `read-model/shift-detail.ts` — de #91-les: nooit ruwe enums). Complement van `shifts.find`/`suggest_chefs` + `hours.detail`. Golden-case **G-shift**. | ✅ SHIPPED (type-check 0 · tools 193/0 · safety 71/0 · eval **27/27** incl. G-shift · live geverifieerd op dev: "ingevuld, bezetting 2/2, team bevestigd" — schone NL-labels). Census **51 tools** (32 read / 13 act / 6 personal). |
| **Tool: chefs.history_at_client** (#95) | `chefs.history_at_client(chefId, clientId)` (tool #52, chefs.read) — de staat van dienst van één chef bij één klant: hoe vaak gewerkt, rollen, laatst, **no-shows/annuleringen**, en de interne beoordeling (gem. ster + tags + opmerkingen). Voor de plaatsingsbeslissing "zal ik X wéér naar Y sturen?". Benut de ratings + (binnenkort) placement_comments-data → "betere beslissingen". Owner-only (ratings intern V1; chef/klant-PA's krijgen 'm niet). `read-model/chef-client-history.ts`. | ✅ SHIPPED (type-check 0 · tools 196/0 · safety 72/0 · eval **28/28** incl. G-hist → chefs.find+clients.find **parallel** · live-valid op dev). Census **52 tools** (33 read / 13 act / 6 personal). |
| **Matching-intelligentie** (#96, fork-keuze 1) | Playbook-verrijking: bij een plaatsingsvoorstel pakt de assistent nu óók `chefs.history_at_client` voor de topkandidaten erbij (trackrecord bij déze klant — keer gewerkt, beoordeling, no-shows) en noemt dat per aanbeveling; + directe route voor "hoe deed chef X het bij klant Y". De fusie was al *emergent* (suggest_chefs + history_at_client via parallel tool-calls); deze regel maakt 'm **betrouwbaar**. Geen algoritme-wijziging (ranking ongemoeid). | ✅ SHIPPED (type-check 0 · eval **28/28** — geen routing-regressie van de prompt-wijziging). |
| **Write-tool: contacts.log** (#97, fork-keuze 2) | **Eerste schrijf-/act-tool van deze sprint:** `contacts.log` (tool #53, **act**, chefs.write) — leg een contactmoment vast bij chef/klant (kanaal · uitkomst · notitie) → schrijft `contact_logs` (verschijnt in `contacts.timeline` + telt mee in de relatie-historie). Voor "noteer dat ik Daniel net belde, hij kan zaterdag". **Confirm-gated** (risk `outbound` → één bevestiging; de confirm-preview toont doel + kanaal + notitie). Action `actions/log-contact.ts` (spiegelt de app-`logContact`). | ✅ SHIPPED (type-check 0 · tools 200/0 · safety 75/0 incl. confirm-gate-check · eval **29/29** incl. G-log · **live write+read+cleanup op dev** · lint+build groen). Census **53 tools** (33 read / **14 act** / 6 personal). |
| **Proactieve intelligentie: risks.scan** (#98, fork-keuze 3) | `risks.scan` (tool #54, cockpit.read) — vooruitblik op "wat kan deze week misgaan", op ernst gesorteerd: (1) **onderbezetting** — open/aanvraag-diensten binnen 7 dagen, (2) **verlopende papieren bij ingeroosterde chefs** — doc verloopt terwijl de chef een bevestigde toekomstige dienst heeft (kan straks niet werken), (3) **vastgelopen uren** — ingediend >5d niet getekend / concept >3d niet ingediend. `read-model/risks.ts`. Voor "waar moet ik op letten?". | ✅ SHIPPED (type-check 0 · tools 203/0 · safety 76/0 · eval **30/30** incl. G-risk · live op dev: "1 risico [hoog] onderbezetting — 4 open diensten binnen 7 dagen"). Census **54 tools** (34 read / 14 act / 6 personal). |
| **Dagstart toont risico's** (#99, fork-keuze 3 afgerond) | `buildDailyBriefing` roept nu ook `scanRisksForAi` aan en voegt een **"⚠️ Risico's (vooruit)"**-sectie toe (🔴 hoog / 🟠 middel, top-3) — de proactieve risico-intelligentie wordt nu élke ochtend mee-gepusht (niet alleen on-demand via `risks.scan`). `hasUrgent` telt hoge risico's mee; `data.risks` toegevoegd. | ✅ SHIPPED (type-check 0 (mijn files) · smoke-briefing **9/0** met risks · live dagstart toont de Risico's-sectie: "🔴 4 open diensten binnen 7 dagen"). |
| **Efficiëntie: prompt-caching gefixt + output-cap** (#100, "faster/cheaper") | Uit de **efficiëntie-audit**: de statische prefix is ~8k tokens/turn (72% = de 54 tool-defs), maar caching werd **gesloopt** doordat `pageBlock`+`memoryBlock` vóórin de system-message zaten. Fix: (1) brain krijgt de **statische** `DEFAULT_SYSTEM_PROMPT`; page/memory-context rijdt nu als **trailing system-message** mee (prefix = system+tools blijft byte-stabiel → cachebaar) · (2) **`prompt_cache_key: owner:<userId>`** per gebruiker · (3) **`max_completion_tokens: 2000`** (output = 6× input-prijs; begrenst kosten + tail-latency). `Msg.role` kreeg `"system"`. **~40-55% input-kosten omlaag op cache-hits.** | ✅ SHIPPED (type-check 0 · spine 36/0 · brain 28/0 · stub-payload: cache-key+cap aanwezig, system éérst / context laatst · **live: OpenAI accepteert de velden voor gpt-5.4** ("Hallo!") · lint+build groen). ⏳ Vervolg: `cached_tokens` uit de response op de kosten-kaart tonen (bewijst de winst + corrigeert de overschatting). |
| **Coverage: intake.list + chefs.availability** (#101, "stronger") | Uit de **coverage-audit** (grootste blinde vlek = de assistent zag wel *aantallen* wachtend werk maar kon de items niet openen). Twee reads erbij: **`intake.list`** (tool #55, inbox.read) — de intake-inbox: nieuwe/te-triëren chef-sollicitaties (naam·rol·ervaring·plaats) + klant-aanvragen (bedrijf·rol·wanneer·aantal), status nieuw/bekeken · **`chefs.availability`** (tool #56, chefs.read) — doorgegeven beschikbaarheid van één chef (geblokkeerde + expliciet-vrije dagen, komende 30d). `read-model/intake.ts` + `chef-availability.ts`. | ✅ SHIPPED (type-check 0 · tools 209/0 · safety 78/0 · eval **32/32** incl. G-intake (direct) + G-avail (entity-first) · live op dev: intake 1 open aanmelding, availability degradeert netjes). Census **56 tools** (36 read / 14 act / 6 personal). |
| **Coverage: klant-verzoeken queue (read + decide)** (#102, "stronger") | De owner-goedkeur-inbox voor klant-verzoeken op bevestigde diensten was onzichtbaar (een hotel dat wil annuleren = urgent). **`clients.shift_requests`** (tool #57, shifts.read) — openstaande wijzigings-/annuleringsverzoeken, eerstvolgende dienst eerst · **`clients.decide_shift_request`** (tool #58, **act**, shifts.write, tier **financial**) — keur goed/af; wrapt de geteste `decideShiftChangeRequest` (atomair + klant-uitkomstmail + notificatie; goedgekeurde annulering annuleert de dienst écht). `read-model/client-requests.ts`. | ✅ SHIPPED (type-check 0 · tools 216/0 · safety 82/0 incl. financial-confirm-gate · eval **33/33** incl. G-req · live-valid op dev). Census **58 tools** (37 read / **15 act** / 6 personal). |
| **Coverage: placements.reply (klant-hub)** (#103, "stronger") | De assistent kon de klant-hub (de single source of truth) niet beantwoorden — "reageer op de klant" had geen correct pad. **`placements.reply`** (tool #59, **act**, shifts.write, outbound) — plaatst een bericht in de plaatsing-gesprekslijn namens Chef & Serve, `client_visible` (klant ziet 't op de dienstpagina) of `internal`. Wrapt de geteste `addPlacementComment` (de ENIGE juiste weg voor multi-actor comments — nooit `placements.notes`). `shifts.detail` geeft nu het `placementId` per teamlid zodat de assistent kan richten. | ✅ SHIPPED (type-check 0 · tools 220/0 · safety 85/0 · eval **34/34** incl. G-reply · **live comment-write+cleanup op dev**). Census **59 tools** (37 read / **16 act** / 6 personal). |
| **Chaos-robuustheid (vaagheid)** (#104) | Playbook-sectie "Omgaan met vaagheid en chaos": vergeten naam/vage omschrijving → zélf zoeken via find/semantic_search + voorstellen wie hij bedoelt (nooit blokkeren op een ontbrekende naam); rommelige meerdelige opdracht → álle onderdelen vangen + later-toegevoegde wensen aan dezelfde taak hangen + kort opsommen; van-onderwerp-springen → rode draad vasthouden; pas na zelf-zoeken één korte gerichte vraag. Antwoord op Maarten's vraag "hoe vaag kan ik zijn en nog op de AI rekenen". | ✅ SHIPPED (type-check 0 · eval **34/34** — geen routing-regressie). |
| **PDF-analist: reports.business_kpi** (#105, vlaggenschip) | De AI genereert nu een **echt PDF-bedrijfsrapport** (KPI-dashboard): omzet & marge (maand + YTD + **6-maanden SVG-staafgrafiek**), bezetting, actieve chefs, operationele aandachtspunten — met **uitleg in gewone taal** ("Wat dit betekent"). Tool **`reports.business_kpi`** (tool #60, cockpit.read) → `read-model/report-kpi.ts` (snapshot + maand-tijdreeks uit shift_hours) → `reports/kpi-report.tsx` (`@react-pdf/renderer`, hand-getekende SVG-grafiek, géén externe chart-service) → `reports/render.tsx` (render→R2 `putObject`→presigned 24u-downloadlink). Nieuwe dep `@react-pdf/renderer`. Antwoord op Maarten's "maak me een amazing rapport in PDF met visuele data". | ✅ SHIPPED (type-check 0 · tools 223/0 · eval **35/35** incl. G-report · **live render geverifieerd: geldige PDF `%PDF-`, 4398 bytes** · **build exit 0** — react-pdf bundelt server-side · R2-upload via de bewezen privacy-export-helpers → werkt in prod; dev zonder R2-creds degradeert netjes). Volgende rapporten (chef/klant) kunnen het template hergebruiken. |
| **PDF-rapport #2: reports.chefs** (#106) | Tweede rapport op dezelfde pipeline (Maarten's letterlijke voorbeeld "maak me een rapport over de chefs"): **`reports.chefs`** (tool #61, cockpit.read) — per chef omzet/marge/inzet (uren·diensten) over een venster (std 90d, instelbaar), met **top-chefs-staafgrafiek + volledige tabel + uitleg**. `read-model/report-chefs.ts` (wrapt de geteste `getChefRevenueBreakdown` over de metrics-snapshots) → `reports/chefs-report.tsx`. `render.tsx` ge-refactored: gedeelde `uploadPdf()` + per-rapport generator. | ✅ SHIPPED (type-check 0 · tools 226/0 · eval **36/36** incl. G-report-chefs · **live render: geldige PDF `%PDF-`, 3476 bytes** (0 chefs in dev → nette lege-staat) · build exit 0). Census **61 tools** (39 read / 16 act / 6 personal). |
| **PDF-rapport #3: reports.clients** (#107) | Rapport-trio compleet: **`reports.clients`** (tool #62, cockpit.read) — per klant omzet/marge/bezetting over een venster (std 90d), met top-klanten-grafiek + tabel + uitleg. `read-model/report-clients.ts` (wrapt `getClientRevenueBreakdown`) → `reports/clients-report.tsx` (spiegelt het chef-rapport). De AI kan nu KPI-, chef- én klant-rapporten als PDF maken. | ✅ SHIPPED (type-check 0 · tools 229/0 · eval **37/37** incl. G-report-clients · live render: geldige PDF `%PDF-`, 3503 bytes). Census **62 tools** (40 read / 16 act / 6 personal). |
| **Act: intake.convert** (#108) | Maakt de intake-flow rond (read #101 → act): **`intake.convert`** (act, inbox.triage) — zet een chef-/klant-aanmelding om naar een echt profiel; wrapt de geteste `convert{Chef,Client}Submission` (idempotent). Confirm-gated. Voor "zet deze sollicitatie om naar een chef". ⚠ **Coördinatie:** de parallelle PR-INTEL-chat heeft intussen 3 eigen tools in dezelfde registry gezet (`chefs`/`clients.intel_snapshot`, `match.intel`) — daarom toont de gate 245 (66 tools: 63 van mij + 3 intel). | ✅ SHIPPED (type-check 0 · tools 245/0 · eval **38/38** incl. G-convert). Mijn census: **63 owner-tools** (40 read / **17 act** / 6 personal). |
| **AVG-veilige onboarding-chase** (#109) | Maarten's visie "AI detecteert wat mist + jaagt het na, zonder gevoelige info te tonen" (van actief → proactief). **`onboarding.missing`** (read, chefs.read) — sweep van actieve chefs: wie mist nog verplichte AVG/onboarding-velden (BSN/IBAN/ID-kopie/adres/dienstverband/…), minst-compleet eerst. **`onboarding.request_missing`** (act, chefs.write, confirm-gated) — mailt chef(s) PRECIES welke velden nog nodig zijn + portaal-link; bulk = alle incomplete chefs ieder hun eigen lijstje. **AVG-veilig by design:** wrapt `getOnboardingReadiness` (neemt booleans, geeft alléén veld-LABELS — nooit waarden) + de geteste `createProfileDataRequest` (e-mail + contact_log). `read-model/onboarding.ts`. Commit met **expliciete pathspec** (incident-fix #108). | ✅ SHIPPED (type-check 0 · tools 252/0 · eval **39/39** incl. G-missing · live sweep op dev: 7 chefs incompleet, toont labels "BSN, IBAN, ID-kopie" — géén waarden). Mijn census: **65 owner-tools** (41 read / 18 act / 6 personal). ⏳ Vervolg-visie: klant-onboarding (lichter, geen ID/IBAN), owner-notificatie zodra een chef z'n data invult (hook in profiel-opslag), "let's chat"-uitnodiging na form-1, en de 2 onboarding-formulieren (jotform-migratie) — die laatste hebben Maarten's form-spec nodig. |
| **Native KLANT-onboarding (Stage-2, BEDRIJFSGEGEVENS)** (#110) | De klant-JotForm nu nét als de chef-JotForm natief in ons form-builder-systeem. **Belangrijk inzicht:** de CHEF-onboarding bestond al volledig (`chef-onboarding`, 36 system-velden); alleen de KLANT-kant ontbrak. Gebouwd door de chef-pipeline te **spiegelen** (niet generaliseren — chef-writeback ongemoeid): nieuw `client-onboarding`-form (slug, audience client, **6 secties / 51 system-velden**), `forms/client-system-bindings.ts` (target clients/client_contacts/client_documents; geen encryptie — geen bijzondere gegevens), `domain/client-onboarding.ts` (hydrate/buildWriteback/persist/draft/submit — **contact-fan-out** naar 1 client_contacts-rij per rol, non-destructief, post-completion write-gate, lean audit), `domain/client-documents.ts` (RI&E-upload→R2), wizard `app/(client)/client/onboarding/*` (verbatim kopie van de chef-wizard). **Werkcontext-laag** toegevoegd (type bedrijf/rollen/diensten/voertaal/meenemen — operationele FACTEN). **Billing uitgesloten** (invoicing-team) + **judgment = Layer 2** (`clients.intel`, intel-team — coordineren, niet hier gebouwd). Migratie **0044** (additief: 4 enums + 2 enum-values + ~31 clients-kolommen + client_contacts.title + uniek (clientId,role)-index + client_documents-tabel; **geen DROP/ALTER** van invoicing/intel). | ✅ SHIPPED (type-check 0 · lint+build groen, `/client/onboarding` route gebouwd · migratie 0044 op dev toegepast · seed: client-onboarding 51 velden · **live E2E op dev**: submit schrijft kolommen+Werkcontext-arrays+rechtsvorm, **contact-fan-out** (1 rij/rol), **contact-volledigheid-guard** (finance-naam zonder e-mail geblokkeerd), idempotente re-submit, **2 consent-rijen** (klant + bedrijfsgegevens) · AVG-veilig: geen waarden gelogd). Pathspec-commit (#108-fix). ✅ **Migratie 0044 + seed:forms toegepast op PROD** (ep-icy-scene, 2026-06-09): clients-kolommen + client_documents + enum-values geverifieerd, `client-onboarding` published (6 secties/51 velden) — #110/#111/#113/#114 nu live op prod. ⚠ **Prod-migreren-gotcha**: drizzle.config + seed-forms hardcoden `config({path:".env.local"})` (dev) en lezen `DATABASE_URL_UNPOOLED ?? DATABASE_URL` → forceer `DATABASE_URL_UNPOOLED` (niet alleen `DATABASE_URL`) in de shell, anders migreer je per ongeluk DEV. Branch-check (`ep-icy-scene` ≠ dev `ep-green-mouse`) vóór elke prod-write. |
| **Klant-onboarding: CTA + owner-notify** (#111) | Maakt #110 bruikbaar + levert de visie "AI meldt owner zodra klant z'n gegevens invult": (1) **CTA-banner** op het klant-dashboard (`(client)/client/page.tsx`) → link naar `/client/onboarding`, getoond tot `onboardingStatus="submitted"` (draft-bewuste tekst). (2) **Owner-notificatie** bij submit: `submitOnboarding` (client) stuurt na consent een `createNotification` naar de owner ("Klant X heeft bedrijfsgegevens ingevuld" → `/admin/business/clients/[id]`), best-effort (blokkeert submit nooit). Geïsoleerd — chef-pad/billing/intel ongemoeid. | ✅ SHIPPED (type-check 0 · build groen). |
| **Klant-onboarding: "zelfde als algemeen contact"** (#112) | Contactsectie van de klant-wizard krijgt een kopieer-checkbox per secundaire rol (Financieel contact, Tekenbevoegde): kopieert het algemene contact (naam/functie/tel/e-mail) naar die rol + houdt het gesynchroniseerd tijdens bewerken + lockt de gekopieerde velden ("Gekoppeld aan algemeen contact"). Gematcht op `systemKey` (`client.contact_<rol>_<deel>`) → alleen actief op het klant-form. Backend onveranderd (nog steeds 1 client_contacts-rij/rol). | ✅ SHIPPED (type-check 0 · build groen). |
| **Proactieve AI: klant-onboarding gap-detectie** (#113) | De "active→proactive" visie, klant-kant: de AI kan nu zien **welke klanten nog bedrijfsgegevens missen** (de chef-kant bestond al: `onboarding.missing`). Gespiegeld op het bewezen chef-patroon: `getClientOnboardingReadiness` (profile-completeness.ts — additieve sibling, label-gebaseerd, **geen PII**, **geen billing**), read-model `ai/read-model/client-onboarding.ts` (`sweepClientOnboarding`/`clientOnboardingStatus` — leest clients-kolommen + client_contacts-rollen + client_documents-types), AI-tool `client_onboarding.missing` (read, `clients.read`) geregistreerd in tools/index. Geeft alléén **welke velden** ontbreken (labels: bedrijfsnaam/bezoekadres/KvK/BTW/rechtsvorm/algemeen contact/tekenbevoegde/RI&E), minst-compleet eerst. | ✅ SHIPPED (type-check 0 · smoke-ai-tools 255/0 · smoke-ai-safety 99/0 · lint+build groen · **live dev**: sweep → 6 incomplete klanten, correcte scores + labels, geen PII). ⏳ Vervolg: proactieve **nudge-worker** (klant/chef in-app + owner-samenvatting, dark-launch) · `client_onboarding.request_missing` (gated outbound, via `recipientsForClient`). |
| **AVG-transparantie: "welke gegevens hebben we van jou?"** (#114) | De visie "een duidelijke pagina met AVG-data": `/client/privacy` had al het *verzoek*-formulier (inzage/export/correctie/verwijdering, PR-AVG-1); nu ook de **transparantie-inventaris** ernaast. Nieuwe pure server-component `components/privacy/ClientDataOverview.tsx` rendert de eigen bedrijfsgegevens read-only, gegroepeerd per onboarding-sectie (label: waarde), via `getPublishedForm`+`hydrateFormState` (hergebruik). Cross-link naar `/client/onboarding` om aan te passen. **Billing bewust afwezig** (invoicing-team). Klant ziet z'n EIGEN data (transparantie, geen lek). Minimale, additieve edit aan de PR-AVG-1-pagina. | ✅ SHIPPED (type-check 0 · lint+build groen, `/client/privacy` gebouwd). |
| **Proactieve onboarding-nudge-worker** (#116) | Frontier #1/4 van "AI van actief → proactief" (owner koos "all 4"). Autonome onboarding-chase: thin worker `workers/onboarding-nudge.ts` (wekelijks, ma 09:00, dark-launch via `ONBOARDING_NUDGE_ENABLED`) POST't de app-side cron-route `app/api/cron/onboarding-nudge/route.ts` (Bearer `CRON_SECRET`, `@/`-read-models). De route sweept incomplete **chef + klant** onboarding (`sweepChefOnboarding`+`sweepClientOnboarding`, labels-only/geen PII), **nudge't elke chef/klant in-app** (alleen wie een userId heeft — demo/no-user wordt geskipt) naar `/chef|client/onboarding`, en stuurt **Maarten een "wie mist wat"-samenvatting**. **Per-user throttle 6 dagen** (recency-query op notifications) — geen spam bij re-run. In-app ONLY (geen outbound mail — veilig dark-launch). Patroon = daily-briefing (thin worker → cron-route). | ✅ SHIPPED (type-check 0 · workers tsc 0 · build groen · **live dev E2E**: run 1 → 1 klant genudged + owner-samenvatting; run 2 → 0 (throttle bevestigd)). ⏳ Vervolg: outbound e-mail-nudge (gated, via `recipientsForClient`/profile-data-request) · §6 decision-point auto-drafts. |
| **AI chaos-robustness (eval + playbook)** (#117) | Frontier #4/4: "hoe vaag/chaotisch mag Maarten zijn?". `scripts/eval-ai.mts` uitgebreid met een **CHAOS-sectie** (12 cases: typo's, geen interpunctie, multi-intent, vage persoonsverwijzing, NL/EN-mix, telegram-stijl, hardop-denken) + een vage-destructieve SAFETY-case ("annuleer gewoon alles van zaterdag" → moet eerst opzoeken, niet naar placements.cancel springen). Onthulde 1 echte mis-route (vage "welke chef?" → `knowledge.search` i.p.v. entiteit-zoektools) → **playbook-regel** toegevoegd die entiteit-zoek (chefs.semantic_search/history_at_client/clients.find) scheidt van knowledge.search (genoteerde kennis). | ✅ SHIPPED (type-check 0 · **eval-ai 52/52 groen** incl. alle 12 chaos + 11 safety; safety-net houdt onder chaos: injectie geweigerd, vage-cancel zoekt eerst op). |
| **AI demand-forecast (bezetting vooruit)** (#118) | Frontier #3/4: "je komt week 28 vier sous-chefs tekort". Read-model `ai/read-model/demand-forecast.ts` (`buildDemandForecast`) telt openstaande plekken (`headcount − bevestigde plaatsingen`) over álle aankomende diensten (niet cancelled/completed), gegroepeerd per **ISO-week + rol**; geeft `rows`/`shortfalls`/`totalOpen`. Forward FEITEN (echte toekomstige diensten + fill-state), géén ML-voorspelling. AI-tool `demand.forecast` (read, `shifts.read`, optioneel `weeks` 1-26 default 6) geregistreerd. Vult het gat tussen `plannerCockpit` (48u/7d) en roster-view (this_month) — niemand deed multi-week shortfall-per-rol. | ✅ SHIPPED (type-check 0 · smoke-ai-tools 258/0 · **eval-ai 53/53** (G-demand routeert) · build groen · **live dev**: 8 open plekken/8wk → "week 24: 4× sous-chef, week 25: 2× sous-chef …" — precies de visie). |
| **AI inbound-comms (Resend inbound)** (#119) | Frontier #2/4 + "de AI ziet de mail van een chef/klant": chef/klant-e-mail die BINNENKOMT wordt gevangen, geclassificeerd en gerouteerd. Migratie **0045** (`inbound_messages`-tabel + `inbound_category`-enum: question/complaint/urgent/other — additief, los van outbound `email_messages`/`email_events`). Webhook `app/api/webhooks/resend-inbound/route.ts` (svix-geverifieerd met `RESEND_INBOUND_SECRET`, **dark-launch** → 503 tot secret+MX geconfigureerd; landt rauwe payload in `webhooks_received`). Domain `domain/inbound.ts`: match afzender → chef/klant (chefs.email/clients.email/client_contacts.email), heuristische classificatie (klacht/spoed-regex), opslaan (dedup op providerMessageId), **Maarten notificeren** bij bekende afzender óf klacht/spoed (spam van onbekende blijft stil). AI-tool `inbound.list` (read) — afzender+onderwerp+classificatie, **NOOIT de rauwe body** (afzenderinhoud = onbetrouwbaar, blijft uit de model-context). | ✅ SHIPPED (type-check 0 · smoke-ai-tools 261/0 · **eval-ai 54/54** (G-inbound routeert) · lint+build groen · migratie 0045 op dev · **live dev E2E**: klacht→category complaint+notified, dedup bevestigd, list zonder body). ✅ Migratie 0045 op PROD toegepast (ep-icy-scene, 2026-06-09; `inbound_messages` geverifieerd). **Hardening (#120, svix-E2E met échte signatures):** (a) sender-match **case-insensitive** (`lower(col)` — DB bewaart mixed-case e-mails; ontdekt doordat demo-klant niet matchte), (b) handler aangepast aan het ÉCHTE `email.received`-payload (docs: **metadata-only** — `email_id`/bare `from`/`to`/`subject`/`message_id`, GEEN body) + best-effort body-fetch `GET api.resend.com/emails/receiving/{id}` (timeout 5s; **send-only restricted key → 401 → graceful subject-only classificatie**; body stroomt zodra de key receiving-read krijgt). Real-shape E2E: match+urgent+notified in 571ms; forged svix → 401. ⚠ **Owner-activatie (Resend dashboard, key is send-only dus geen API-config)**: Receiving-tab → `.resend.app`-testadres (geen DNS) of eigen domein+MX → webhook `email.received` → `https://<prod>/api/webhooks/resend-inbound` → signing-secret als `RESEND_INBOUND_SECRET` op Vercel. Adres/key later wisselen = alleen env, geen code. ⏳ Vervolg: `inbound.read` (1 bericht, body als DATA) + LLM-classificatie + outbound reply-draft + inbox-UI. |
| **CLAUDE.md herschreven + path-scoped `.claude/rules/`** (#121) | Volledige refresh naar de actuele officiële Claude Code memory-richtlijnen (juni 2026; geverifieerd via code.claude.com/docs/en/memory — geen aparte "Fable 5"-richtlijnen, Fable 5 is het model, gelanceerd 2026-06-09). **Kern-CLAUDE.md** (77 regels, binnen de 80-120-sweet-spot): stale claims gefixt (AI-laag "not yet built" → LIVE met 76 tools; migraties 0024 → 0045; 15 workers; stale plan-pointer weg) + 4 nieuwe load-bearing hard rules (untrusted content = DATA · pathspec-commits bij parallelle chats · prod-DB `DATABASE_URL_UNPOOLED`+branch-check · dark-launch-conventie). **Nieuw mechanisme**: `.claude/rules/{ai-work,db-and-migrations,workers}.md` met `paths:`-frontmatter — laden LAZY alleen wanneer matchende bestanden worden aangeraakt (contextbesparing per sessie): AI-toolconventies+gates, migratie/prod-procedure, thin-ticker/webhook-conventies. `.claude/settings.local.json`/`launch.json` bewust NIET gecommit. | ✅ SHIPPED (docs-only; 187 regels totaal over 4 bestanden). |

### In-progress / next

| PR | Description | Status |
|---|---|---|
| PR-CHEF-0 | Integration spine (outbox/runs/external_refs/email tracking/notifications/contact_logs) | ✅ live (migration 0011 applied · 18/18 smoke pass · /admin/business/integrations live) |
| PR-AI-0 | AI/RAG docs spine (docs/ai/) | ✅ live (28 docs, 7,230 lines — 11 base files + 10 workflow playbooks + 6 tool-contract files + README) |
| PR-CHEF-1 | Hours chain (chef → klant → admin → exported) + trust timeline | ✅ live (migration 0012 applied · 9 emails · trust timeline · chef form · klant receipt · admin queue+detail) |
| PR-CHEF-2 | Three role dashboards ("wat moet ik nu doen?") | ✅ live (chef + klant + admin all "actie nodig" first + integration health card + ActionCard shared) |
| PR-CHEF-3 | Admin bulk approval + dispute UX + manual-add hours | ✅ live (magic eligibility + bulk-select bar + per-row approve + anomaly flagging in queue) |
| PR-CHEF-4 | Profile editing (direct + request-change split) | ✅ live (migration 0013 · ProfileForm direct edit · RequestChangeFormSection for sensitive fields · profile_change_requests table) |
| PR-CHEF-5 | Confirm-notify chef + cancel-severity + contact cards | ✅ live (ShiftConfirmedChefEmail · ShiftCancelledByChefClientEmail · cancellation-severity util · cancel flow with tel: CTA · contact card · rejection reason on accept/reject) |
| PR-CHEF-6 | Notification prefs scaffolding | ✅ live (migration 0014 · notification_prefs table · shouldSendToUser/setPref helpers · V1 always-on) |
| PR-CHEF-7 | Payroll batches + corrections + CSV export | ⏳ |
| PR-CHEF-8 | Email delivery tracking (Resend webhooks) | ✅ live (POST /api/webhooks/resend with svix-signature HMAC verify · recordEmailEventFromWebhook updates email_messages.status) |
| PR-CHEF-9 | In-app notification inbox UI | ✅ live (bell with unread badge in all 3 layouts · /chef/notifications · /client/notifications · /admin/notifications · markRead + markAllRead) |
| PR-CHEF-10 | AVG consent + privacy requests + retention policies | ✅ live (migration 0018 · consent_log + privacy_requests + DPA + retention_policies · ConsentGate modal flag-gated · /privacy-chef + /privacy-klant placeholders) |
| PR-CHEF-11 | Calendar ICS feeds (chef + klant) | ✅ live (migration 0015 calendar_token · src/lib/calendar/ics.ts · /chef/calendar.ics + /client/calendar.ics public-by-token · /chef/calendar + /client/calendar UI with copy URL + rotate secret) |
| PR-CHEF-12 | Document verification + expiry + visibility | ✅ live (migration 0016 · clientVisible+verifiedAt/By+expiresAt+status on chef_documents · /api/chef-document/[id] 3-way access · workers/document-expiry.ts · profile doc list with visibility chips) |
| PR-CHEF-13 | Backup + restore drill + encryption | ✅ live (migration 0017 · backup-neon.sh + restore-drill.sh + backup-install.sh + launchd plist + age-encryption support + 12-week retention · backup_runs + restore_drills tables) |
| PR-CHEF-7 | Payroll batches + corrections + CSV export | ✅ live (migration 0019 · payroll_batches + lines + shift_hour_corrections · /admin/business/payroll + CSV export route) |
| PR-CHEF-14 | Polish: countdown, empty/late states, doc visibility labels | ✅ folded into PR-CHEF-2 (countdown/earnings) + PR-CHEF-12 (doc labels) + PR-CHEF-5 (rejection reason) |
| PR-CHEF-15 | Web Push (DEFERRED) | 💤 deferred |
| PR-CHEF-FUT | Reserved API/webhook schemas (no UI) | 💤 reserved |
| PR-PLANBORD-1 | **Drag-drop planbord** (`/admin/business/roster/planbord`, gate `shifts:write`) — concept-rooster met **publish-gate**: sleep chef → **DRAFT**-plaatsing (onzichtbaar voor chef+klant + ICS, telt niet mee in shift-status), focus-rail rangschikt matches met de "waarom"; **"Publiceer week"** flipt draft→proposed (her-valideert dubbele boekingen wég) + mailt iedereen via de geëxtraheerde `sendProposalNotifications`. AI: confirm-gated `roster.publish` ("publiceer maar") + draft-aware `roster.overview` (`draftsPending`). Engine: `draftPlacement`/`publishDraftsForPeriod`/`removeDraftPlacement` (`domain/roster-publish.ts` + `matching.ts`); `@dnd-kit/core`. | ✅ **dev-verified** (migration 0039 `draft` enum op DEV-branch · `smoke-planbord.mts` **13/0** · type-check+lint+**build** groen). ✅ **SHIPPED TO PROD** (migratie 0039 op prod `ep-icy-scene` toegepast + merge to main `522cfab` + Vercel-deploy; UI/auth-render nog te eyeballen) |
| PR-PLANBORD-2 | **Publiceren = weekoverzicht + agenda.** Publish stuurt nu **ÉÉN digest per chef** (zijn week + **adres + contactpersoon + telefoon + chef-zichtbare details**) en **ÉÉN per klant** (de chef **met telefoonnummer**) i.p.v. N losse voorstel-mails — beide met een **`.ics`-bijlage** voor de agenda. `sendEmail` kreeg attachments-support; `ChefWeekPlanningEmail`/`KlantWeekPlanningEmail` + `sendWeekDigests` in `roster-publish.ts`. AVG: chef-details uit `chef_visible_notes`, nooit interne `notes`. | ✅ dev-verified (type-check+lint+build+`smoke-planbord` 13/0 groen; mails best-effort). ✅ **SHIPPED TO PROD** (`522cfab`); ⏳ e-mail-render nog eyeballen (echte testmail) |
| PR-PLANBORD-3 | **"Vul de week" (AI drafts the week).** Greedy autofill over `findMatchesForShift`: zet de best passende beschikbare chef als **concept** op elke open plek — per slot her-query → géén dubbele boekingen / niet 2× op één dienst; lichte fairness-spreiding. Knop op het planbord (`autofillWeekAction`) + confirm-gated AI-tool **`roster.autofill`** ("vul de week"). `domain/roster-autofill.ts`. Deterministisch nu; LLM-scorer schuift later achter `findMatchesForShift` zónder UI-wijziging. | ✅ dev-verified (type-check+lint+build groen · `smoke-planbord` **16/0** incl. autofill · `smoke-ai-tools` **162/0** incl. roster.publish+autofill). ✅ **SHIPPED** (`522cfab`) |
| PR-PLANBORD-4 | **"Per chef"-lens op het planbord.** Toggle Per dag / Per chef: de chef×dag-pivot (`ChefWeekGrid`) toont elke actieve chef × 7 dagen met wat-ie-doet per dag (status-kleur), een **belasting-badge** en **vrije chefs grijs + geteld** ("X chefs nog vrij deze week") — read-only inzicht ("wie is vrij?"). `chefId` door de slots gedraad. | ✅ dev-verified (type-check+lint+build groen). ✅ **SHIPPED** (`522cfab`) |
| PR-PLANBORD-5 | **Beschikbaarheid op de "Per chef"-lens.** `chef_availability` (geblokkeerde dagen) nu zichtbaar in de pivot: geblokkeerde dag = "niet beschikbaar"-cel, en de **"vrij"-badge betekent nu écht vrij** (geen plaatsing én niet geblokkeerd); header telt chefs mét vrije dagen. Plus smoke-dekking voor publish's **blocked re-validatie** (overslaan + reden 'blocked'). | ✅ dev-verified (type-check+lint+build groen · `smoke-planbord` **18/0** incl. blocked-skip). ✅ **SHIPPED** (`522cfab`) |
| PR-PLANBORD-6 | **Build→review→publish afgerond.** "**Wis concepten**"-knop (`clearDraftsForPeriod` — wis alle week-concepten, "opnieuw" na een autofill) + de **match-score op concept-slots** (`Sander · 85` op draft-pills, zodat de planner ziet wáárom een autofill-pick landde). | ✅ dev-verified (type-check+lint+build groen · `smoke-planbord` **20/0** incl. clearDrafts). ✅ **SHIPPED** (`522cfab`) |
| PR-PLANBORD-7 | **Reistijd-hint in de match-rail.** Elke gerangschikte kandidaat toont nu zijn **afstand tot de locatie** (`estimateTravel`, chef-coörd → shift-coörd) — zodat de planner een nabije good-enough chef kan verkiezen boven een verre topscore. Afstand-only (modus-onafhankelijk). | ✅ **SHIPPED** (`c574578`) |
| PR-PLANBORD-8 | **Marge per kandidaat (owner-only).** De rail toont per kandidaat de geschatte **brutomarge** (klantomzet − loon − reis, via `estimateMargin`; loon = `chefs.hourlyRateMinCents ?? shift.chef_rate`) met tone-kleur — zo ziet de owner "beste chef ≠ meest winstgevend". Gegate op **`payroll.read`** (`hasPermission`) zodat planners alleen score + afstand zien, niet de tarieven. | ✅ **SHIPPED** (`2e3e5ef`) |
| PR-EMAILS-COPY | **Geperfectioneerde e-mailcopy + CTA's + copywriter-gids.** Alle **28** transactionele templates herschreven naar kalme, menselijke, action-first tekst (chef warm/praktisch · klant rustig/professioneel · admin kort · security/privacy serieus); elke statusmail eindigt met een duidelijke vervolgstap, ratings zeggen **"feedback"** (nooit "review/beoordeling"). Props/`<EmailLayout>`/`styles` onaangeroerd; branch-copy voor `recipientRole`/`recipientKind`/`intent`. **+ 3 ontbrekende CTA's gewired**: optionele `placementUrl`/`hubUrl` op `ShiftConfirmed{Chef,Client}Email` + `ShiftCancelledByChefClientEmail`, doorgegeven vanaf `placement-transition` / admin-shift-page / chef-cancel. **Copywriter-gids** `docs/EMAIL_TEMPLATES.md` + `scripts/render-emails.mts` → `previews/emails/*.html` (28 + index). | ✅ **SHIPPED** (`9a8de47`+`e2a61ce`; type-check+lint+build groen · 28/28 gerenderd) |
| PR-PLANBORD-CONFIRM | **Batch-confirm + acceptatie-voortgang op het planbord.** Na publiceren accepteren chefs zelf; de planner ziet nu een **voortgangsstrip** (X voorgesteld · Y geaccepteerd · Z bevestigd) en een **"Bevestig (N)"-knop** die álle geaccepteerde plaatsingen van de week in één keer bevestigt (`confirmAcceptedForPeriod` → `transitionPlacement` per rij → chef+klant-bevestigingsmail mét de nieuwe CTA's). `confirmWeekAction` + pipeline-counts uit de page. Sluit de lifecycle: concept → publiceer → accept → **bevestig**. | ✅ verified (type-check+lint+build groen · `smoke-planbord` **25/0** incl. batch-confirm + copyLastWeek + cancel-clears-drafts) |
| PR-OPS-4AREAS | **Vier werkstromen samen geleverd (commit `27707b4`).** **(A) Facturatie** — `invoices`+`invoice_lines` (0040/0041), `generateInvoiceForPeriod` (idempotent per (klant,periode), factureert admin_approved uren op dienstdatum, 21% btw, billing-snapshot) + `sendInvoice`/`markInvoicePaid`/`voidInvoice` (atomair+geaudit), `InvoiceKlantEmail` + `invoice_sent`-routing, admin lijst/detail+acties (`/admin/business/invoices`, gate `invoices.read`), klant-portaal (`/client/invoices`; concept/void lekken nooit). **(B) Reporting** — `/admin/business/reporting` (gate `cockpit.read`): platform-tijdreeks (omzet/marge/bezetting, week/maand) live geaggregeerd uit `client_metrics_daily` (géén nieuwe tabel) + ranglijsten + planner-KPI's; `TrendChart`. **(C) Klant-week** — `/client/week` visueel weekrooster (concept-uitgesloten, `getClientShiftLabel`) + **fix** van een bestaande concept-lek + rauwe-status op `/client/shifts`. **(D) Planbord** — afgewezen plaatsingen tonen nu ("↩ N afgewezen"). Bedraad in e-mail+tracking, notificaties, audit, permissions/route-gates, nav. | ✅ **SHIPPED TO PROD** (commit `27707b4` · migraties 0040/0041 op `ep-icy-scene` vóór deploy · type-check+lint+build groen · smokes invoicing **29** · reporting **15** · klant-week **6** · planbord **25** · e-mailrender **29/29**) |
| PR-OPS-POLISH | **Vervolg op PR-OPS-4AREAS (commits `03419c9`·`bdcb06c`·`971b302`·`c2b1c9d`, GÉÉN migratie).** **Facturatie proactief:** `getUnbilledHoursByClient()` → "Te factureren"-werklijst op `/admin/business/invoices` (goedgekeurde, nog niet gefactureerde uren per klant + 1-klik genereren) + nudge op de cockpit. **Reporting verdiept:** omzet/marge-uitsplitsing **per klant én per chef** (`getClientRevenueBreakdown`/`getChefRevenueBreakdown`) + noise-guarded **anomaly-banner** (`detectSwing`: week/maand ≥30% op materiële basis). **Planbord D2/D3:** Mouse+Touch(hold-to-drag, zodat swipe nog scrollt)+Keyboard-sensors + NL screenreader-announcements. **Klant recurring (B4):** `/client/request` "wekelijks herhalen" → `expandOccurrences` (`src/lib/recurrence.ts`, week/2-week tot datum, cap 12) → N triageerbare `client_submissions`. | ✅ **SHIPPED TO PROD** (type-check+lint+build groen · smokes invoicing **33** · reporting **20** · recurrence **9** · klant-week 6 · planbord 25; géén migratie) |
| PR-INTEL | **Chef & klant intel + KPI's + accounting-export (commits `75bb2d2`·`a689d42`·`06cbd58`·`62ea35b`·`95e68af`·`eecbdfc`, GÉÉN migratie).** **Pattern-intel** (`src/lib/domain/intel.ts`, AI-ready — teammate kan tools wiren): `getChefPatterns`/`getClientPatterns` — werkdag-histogram (Amsterdam tz), rol-mix, **verdiend per klant** + lifetime/30d payout, volledige **vaste-chef-lijst** → "Patronen & relaties"-kaarten op chef- + klant-detail (`ChefPatronenCard`/`KlantPatronenCard`). `getPlatformIntelKpis` (tekensnelheid · actieve chefs/klanten) op `/admin/business/reporting`. **Chef-portaal** `/chef/earnings` (eigen verdiensten + patronen, herbruikt `getChefPatterns`). **Facturatie→boekhouding:** invoice **CSV-export** (`/admin/business/invoices/export.csv`, `?status`-filter) + status-filter op de lijst. **C4** weekdigest verrijkt met omzet/marge/te-factureren-KPI's; **C5** omzet-swing-banner op de cockpit. | ✅ **SHIPPED TO PROD** (type-check+lint+build groen · `smoke-intel` **14/0** · workers tsc groen; géén migratie) |
| PR-INTEL-P1 | **Intel-acquisitie Fase 1 — "Maarten's brein" + decline-capture + AI-ready snapshots (commit `e4958ab`, migratie 0042).** Data zónder formulieren: **OBSERVE** (al gebouwd: patterns/reliability) · **NUDGE** (1-tik decline-chips op de chef-afwijzing → `placements.decline_reason`, preference-signaal) · **DICTATE** ("Maarten brein": `chefs.intel`/`clients.intel` jsonb, 6 oordeelsvelden elk — intern + professionele taal — bewerkbare kaarten op chef/klant-detail, later AI-invulbaar uit een brain-dump). **AI-ready read-model** (`src/lib/domain/intel.ts`): `getChefDeclineSignals` + `getChefIntelSnapshot`/`getClientIntelSnapshot` bundelen brein + patterns + decline-signalen + reactivatie als platte data voor AI-tools (teammate-lane). | ✅ **SHIPPED TO PROD** (migratie 0042 op `ep-icy-scene` vóór deploy · type-check+lint+build groen · `smoke-intel` **19/0**) |
| PR-INTEL-P2/P3 | **Intel Fase 2 + 3 — "voor je belt" + match-intelligence (commits `4b1a5ae`·`71c42fc`·`341264d`, GÉÉN migratie).** **P2:** `ChefSnapshotCard`/`KlantSnapshotCard` — de "voor je belt"-glance bovenaan chef/klant-detail (composeert `getChefIntelSnapshot`/`getClientIntelSnapshot`: brein + patterns + decline-signalen + **reactivatie** "X dagen niet gewerkt — bel" + volgende-actie). `getChefPatterns` leidt nu **dagdeel** af (ontbijt/lunch/diner/nacht uit het Amsterdamse startuur). **P3 (match-intelligence, de "missing layer"):** de planbord-match-rail toont bij élke kandidaat de **chef×klant-relatie** — "★ favoriet · 3× hier · 4,5★" uit `getChefClientHistory` — zodat Maarten de bewezen match stuurt. | ✅ **SHIPPED TO PROD** (type-check+lint+build groen · `smoke-intel` **20/0**; géén migratie) |
| PR-INTEL-P4 | **Intel Fase 4 — chef×klant pair-memory + AI-infra (commit `c7d9501`, migratie 0043).** **Schema:** nieuwe `match_intel` (chef×klant uniek: `note` + `would_rehire`/`would_return` + `ai_why_works`/`ai_why_fails` + `updated_by`) + `placements.chef_return_signal` (post-shift duim). **Domein** (`src/lib/domain/intel.ts`): `getMatchIntel` (pair-row + `getChefClientHistory` + post-shift-duimen) + `saveMatchIntel` (upsert). **AI-infra op de bestaande tool-struc:** read-model `src/lib/ai/read-model/intel.ts` (`chefIntelForAi`/`clientIntelForAi`/`matchIntelForAi` — rollen gehumaniseerd, cents→euro's, brein + AI-why doorgegeven, géén fabricatie) → **3 READ-tools** (`defineTool`, owner-gated, in `ALL_TOOLS`): **`chefs.intel_snapshot`** · **`clients.intel_snapshot`** · **`match.intel`**. De brain redeneert nu over de hele intel-laag (wie is deze chef/klant · past dit paar) vóór een voorstel. ⚠ **Co-edit:** belandde samen met `intake.convert` (#108) in één commit — gedeelde index, gelijktijdige teammate-commit. | ✅ **SHIPPED TO PROD** (migratie 0043 op `ep-icy-scene` vóór deploy + geverifieerd · type-check+lint+build groen · `smoke-intel` **23/0** · `smoke-ai-tools` **241/0** · `smoke-ai-safety` **91/0**) |
| PR-INTEL-P5 | **Intel Fase 5 — acquisitie-UI die de pair-memory vult (commit `7327bc9`, GÉÉN migratie).** Tot nu schreef niets `match_intel`/`placements.chef_return_signal` — de AI las een lege laag. Twee capture-flows sluiten de lus: **OBSERVE/NUDGE** — een 1-tik "Zou je hier terugkomen?" 👍/👎 op de chef z'n eigen, gewerkte, verleden shift (`/chef/shifts/[placementId]`) → `placements.chef_return_signal` (eigenaar-gescoped, intern, aanpasbaar). **DICTATE** — een "Pair-intel · chef × klant"-kaart op de admin shift-detail per geplaatste chef (notitie + "klant neemt weer"-oordeel) → `saveMatchIntel`. **Domein-fix:** `saveMatchIntel` is nu een **PARTIËLE** upsert — alleen meegegeven velden worden geschreven, dus Maarten's notitie wist nooit de AI-velden (`aiWhyWorks`/`aiWhyFails`) en andersom. | ✅ **SHIPPED TO PROD** (géén migratie · type-check+lint+build groen · `smoke-intel` **30/0** incl. partiële-upsert-behoud + duimen) |
| PR-INTEL-P6 | **Intel Fase 6 — pair-intel op het keuzemoment (commit `4702a2e`, GÉÉN migratie).** De vastgelegde pair-memory verschijnt nu wáár Maarten een chef kiest: de "Vul deze dienst"-match-rail (`MatchSuggestions`) toont per kandidaat bij déze klant de **post-shift-duimen** (👍N/👎N), het **"klant neemt weer"-oordeel** en de **notitie-snippet** — naast de bestaande favoriet/× hier/marge-signalen. Twee gebatchte queries (`match_intel` + `chef_return_signal`-tellingen) over de kandidatenset; puur lezen. Sluit de lus capture → keuze. | ✅ **SHIPPED TO PROD** (géén migratie · type-check+lint+build groen) |
| PR-INTEL-P7 | **Intel Fase 7 — proactieve "Maarten's daglijst" (commit `6527d41`, GÉÉN migratie).** Relatie-signalen op `/admin/business/reporting` (`DaglijstCard`): **reactivatie-chefs** (goede chefs die stil vielen — voltooide historie ≥2 maar ≥21d niet gewerkt én niets op de horizon) + **stille klanten** (geboekt maar ≥30d niets). Twee efficiënte aggregaten in `intel.ts` (`getReactivationChefs`/`getQuietClients`); elke rij linkt naar het detail om te handelen. Bewust de **relatie-laag**, niet `risks.scan` (open diensten). | ✅ **SHIPPED TO PROD** (géén migratie · type-check+lint+build groen · `smoke-intel` **35/0**) |
| PR-INTEL-P8 | **Intel Fase 8 — "match-gezondheid" KPI uit de pair-memory (commit `d7522fb`, GÉÉN migratie).** `getMatchHealthKpis` → 3 cockpit-tegels op `/admin/business/reporting`: **chef-tevredenheid** (% post-shift-duimen 👍), **bewezen matches** (paren met `would_rehire=true`) en **pair-notities** (capture-adoptie). Goedkope counts, valt netjes terug op "—" tot er data is. De diepere snapshot-/planbord-herhaling is **bewust uitgesteld** — P2 toont al de relatie-glance, P6 de pair-intel op het keuzemoment. | ✅ **SHIPPED TO PROD** (géén migratie · type-check+lint+build groen · `smoke-intel` **40/0**) |
| PR-INTEL-P9 | **Intel Fase 9 — `match.intel` levert nu het volledige oordeel-bewijs (commit `a0bae6c`, GÉÉN migratie).** `matchIntelForAi` componeert het brein van chef én klant (best ingezet voor · dagdeel · toprollen · wat hij afwijst · beste chef-type · waar ze om geven) ín het bestaande `match.intel`-antwoord — één call = alle context om een koppeling te wegen. Géén nieuwe tool / géén `index.ts`-edit (hete co-edit-zone vermeden). 🤝 **HANDOFF (AI-runtime-lane):** de enrichment-*writer* — LLM die `aiWhyWorks`/`aiWhyFails` schrijft — is voor de AI-chat te bouwen op de réeds geleverde infra: lees via `match.intel` (nu compleet) of de 3 snapshot-tools, schrijf via `saveMatchIntel` (al **partieel** = wist niets anders). Géén nieuwe infra nodig van mijn kant. | ✅ **SHIPPED TO PROD** (géén migratie · type-check + `smoke-ai-tools` **252/0** + `smoke-ai-safety` **98/0** + runtime-bundlecheck groen) |
| PR-INTEL-P11 | **Intel Fase 11 — proven-pair kansen in de daglijst (commit `b2b8686`, GÉÉN migratie).** Het actiemoment waar intel × operatie samenkomen: `getProvenMatchOpportunities` vindt **OPEN** diensten (≤14d) bij een klant die een **bewezen** chef heeft (`match_intel.would_rehire = true`) — "stuur chef Y naar Hotel X, klant neemt hem graag weer" — exclusief geblokkeerde chefs en chefs al op de dienst. Bovenaan de `DaglijstCard` als "Kansen", elke rij linkt naar de shift-detail (P6: pair-intel + 1-klik voorstel). Relatie-geheugen → een invulling. ⚠ **Co-edit:** smoke faalde eerst op de **ongecommitte** `clients.handelsnaam` (PR-CLIENT-ONBOARDING, teammate-werktree, géén migratie) — geverifieerd tegen HEAD-schema (matcht DEV) met trap-restore; mijn code raakt `handelsnaam` niet. (N.B. P10 = `hours-reminders` bleek al gebouwd; alleen stale CLAUDE.md-noot gecorrigeerd, commit `00c3842`.) | ✅ **SHIPPED TO PROD** (géén migratie · type-check+lint+build groen · `smoke-intel` **43/0** · prod-route-smoke 17/17) |
| PR-INTEL-HARDEN | **Hardening-pass (commit `3eba19a`, GÉÉN migratie).** De load-bearing `hours-reminders`-worker (mailt échte chefs/klanten als-ie aanstaat) had **geen test** → `scripts/smoke-hours-reminders.mts` draait de **echte** worker als subprocess tegen geïsoleerde fixtures en bewijst het veiligheidscontract: **gate default-uit** stuurt niets, >24u draft → chef-`24h`-kruimel, >5d submitted → klant-`klant_5d`-kruimel, en een 2e run dupliceert niet (de `alreadySent`-guard). Mail geforceerd uit (`RESEND_API_KEY=""`), raw SQL (immuun voor schema-drift), alles teardown in `finally`. Edge-cases P4-P11 nagelopen (div-door-nul, lege arrays, unique-pair). **Geconsolideerde run: 399 asserts groen samen** (intel **43** · hours-reminders **6** · ai-tools **252** · ai-safety **98**). | ✅ **SHIPPED** (géén migratie · `smoke-hours-reminders` **6/0** nieuw) |

### Klant (hotel) phase — ✅ shipped

| PR | Description | Status |
|---|---|---|
| PR-KLANT-0 | Foundations: shift hub + placement_comments + client_contacts + recipients + AI docs | ✅ live (migration 0020 · /client/shifts/[shiftId] hub · comments.ts visibility-scoped · client-recipients.ts · client-shift-labels.ts · 8 playbooks + 4 tool contracts) |
| PR-KLANT-1 | Profile editing (sectioned, paymentTerms→request) | ✅ live (migration 0021 · /client/profile sectioned: Contactpersoon · Shiftlocatie · Facturatie · request-change · client_change_requests table · admin Wijzigingsverzoeken tab · BillingEmailChangedKlantEmail to OLD address · recipientsForClient outcome email) |
| PR-KLANT-2 | Requests list + cancel + change/cancel for existing shifts | ✅ live (migration 0022 · /client/requests list + retract · shift hub change/cancel modals · client_shift_change_requests + one-open-per-shift-per-kind unique index · submission_status cancelled_by_client · admin inbox decision queue · ClientChangeRequestAdminEmail + ClientChangeRequestOutcomeKlantEmail) |
| PR-KLANT-3 | Chef preview + structured comments + email | ✅ live (no schema · hub proposed-chef card + "Waarom voorgesteld?" reasons via getMatchReasonsForPlacement · ChefFeedbackForm → placement_comments client_visible (NEVER notes) · admin shift-detail comment thread + visibility-scoped reply · proposePlacement adds ChefProposedKlantEmail + chef_proposed notification · klant comment → admin email) |
| PR-KLANT-4 | Recurring templates + exceptions + overnight + preview | ✅ live (migration 0023 · shift_templates + shift_template_exceptions · shifts.source_template_id/date + idempotency index · generate-recurring-shifts worker (Europe/Amsterdam, overnight ends_next_day, ON CONFLICT partial-index) · admin templates list/new/[id] + live preview-before-save + ExceptionsManager + activate toggle · /client/templates friendly view + change-request) |
| PR-KLANT-5 | Rating loop + tags + N≥5 rule + email | ✅ live (migration 0024 · ratings table + chefs.average_rating/rating_count rollup · rating-tags.ts vocab · domain/ratings.ts (submit + recompute + 3 visibility-scoped readers: admin-all / chef-N≥5 / klant-none) · /client/shifts/[shiftId]/rate stars+tags form · RatingPendingKlantEmail + bell + dashboard card on approveHoursRow · admin chef-detail feedback section · chef-profile N≥5 summary) |
| PR-KLANT-DOCS | CLAUDE.md + WORKFLOW link-complete + MEMORY resume-header | ✅ shipped |

### Klant audit-remediation (K1–K4, 2026-06-14) — ✅ shipped

> From a file:line-verified klant-side audit. All merged to main; migration 0056 applied dev + prod (`ep-icy-scene`).

| PR | Description | Status |
|---|---|---|
| K1 (#171) | Audit quick-wins: cockpit CTA `?status=open`→`?tab=open`; **AVG** — dropped email/phone from `findChefs`/`findClients` (`ai/read-model/directory.ts`) so the AI directory never returns contact PII; `ok=rated` rating flash on the klant hub; dashboard `myPending` ActionCard CTA; client nav Agenda+Meldingen entries. | ✅ live (eval green) |
| K2 (#172) | Owner client drill-down — killed the static `Binnenkort` placeholder. `getClientRecentShifts` (`client-history.ts`, shift-centric + grouped-Map, neon-http-safe) → "Recente & aankomende shifts" on `/admin/business/clients/[id]`; "Documenten & onboarding" section (`listClientDocuments` + RI&E readiness chip from `clientDocTypes`); global shifts list `/admin/business/shifts` now accepts **`?clientId=`** (scopes rows/counts/pills/heading). | ✅ live |
| K3 (#174) | Request→shift handoff resolves ("Mijn aanvragen" no longer stuck at triaged). Migration **0056** `client_submissions.converted_to_shift_id`; inbox `[kind]/[id]` "Markeer als opgepakt — omgezet naar dienst" action (atomic new/triaged→converted + optional validated shift link + klant notify); `/client/requests` shows "Omgezet naar dienst →"; `approveHoursRow` zero-rate guard (`reason:"no_rate"`). `smoke-k3` 5/0. | ✅ live (dev+prod migrated) |
| K4 (#174) | AI klant access. Tools **`clients.reachability`** (portal/push/email booleans + recipient COUNT, never addresses — AVG) + **`clients.shift_templates`** (reuses `clientMyTemplates`); **`clients.decide_shift_request`** added to eval `DESTRUCTIVE` + a SAFETY case (model reads `clients.shift_requests` before deciding). Census +2 read tools. `eval-ai` 80/80. | ✅ live (eval green) |

⚠ **K3 lost-commit footgun:** PR #173 squash-merged EMPTY (the K3 branch wasn't ahead of main at merge time — branch-state artifact from `--delete-branch` + reset churn during the K2→K3 hop). Caught by verifying `origin/main` lacked the K3 markers post-merge; recovered the work from the local commit + recombined with K4 into #174. **Lesson:** after a squash-merge, verify the content actually landed (`git show origin/main:<file> | grep <marker>`) — don't trust the "MERGED" state alone.

### Klant audit-remediation Waves (W1–W5, 2026-06-14) — from a 5-dimension adversarial audit

> An exhaustive multi-agent klant-side audit (portal UX · owner mgmt · domain correctness · AI coverage · comms wiring) surfaced 18 confirmed in-lane gaps (+2 cross-lane, flagged not built: client invoice "wat nu?" line + an AI klant-ratings tool → invoicing/clients lane). Built as 5 waves; every PR adversarially diff-reviewed.

| PR | Description | Status |
|---|---|---|
| W1 (#175) | **hours.ts klant-email seam (correctness/AVG).** `approveHoursRow`+`rejectHoursRow` hard-coded `ctx.clientEmail` → now `recipientsForClient()` (role routing + opt-out gate), mirroring `rating_pending`. New `ClientEmailEvent` keys `hours_approved`/`hours_admin_rejected` (transactional/always-on). | ✅ live (review clean) |
| W2 (#176) | **Klant notification + consent integrity on shift-confirm.** Klant `createNotification` added to BOTH confirm cascades (placement-transition.ts + admin shift page; chef already got one); dedicated mutable `shift_confirmed` event (was routed via 'generic'); klant web-push opt-in on `/client/notifications` (reuses parameterized chef `PushOptIn`). | ✅ live (review clean) |
| W3 (#177) | **Owner clients-list triage cockpit.** Batched `getClientHealthVerdicts(ids)` → health-at-a-glance column + health sort; per-klant inbox drill-down (`/admin/business/inbox?clientId=`); `nextActions` ("volgende stap") on `ClientHealthVerdict`/card. `smoke-client-health-batch` 7/0 (batched==single-client). | ✅ live (review fixed 3 sort-preservation bugs) |
| W4 (#178) | **Klant AI `onze.dienst_detail`** — own-data-scoped shift detail (WHERE clientId, client_visible comments only, no internal notes) + read-model + CLIENT_TOOLS reg + prompt. Fixed pre-existing portal-eval rot (tool_calls/parallel). smoke-ai-portal 49/0 (incl. cross-klant IDOR); portal eval 11/11; owner eval 80/80 LOCAL. | ✅ live (merged on local-eval evidence + owner authorization; CI eval gate was OpenAI-quota-blocked → admin-override). See [[openai-quota-gates-eval]]. |
| W5 (#179) | **Glance-layer UX.** Dashboard cards gained "wat gebeurt er nu?" subtitles (`ActionCard.subtitle`); empty-state CTAs on /client/{shifts,week,templates,requests} → /client/request; mobile-scroll header nav (11 items: 1 swipeable row on mobile, wraps on md+). | ✅ live (copy/CSS only) |
| demo seed (#181) | **`seed-demo.ts` extended** — 2 impersonatable login accounts (chef demo-chef@chefandserve.demo + klant demo-klant@chefandserve.demo, linked via userId), 10 past completed shifts → shift_hours (6 approved → omzet/marge, 2 client_signed, 2 submitted), 6 ratings + chef rollup, 1 pending request. Idempotent. **Seeded on dev + PROD** (ep-icy-scene): omzet €1496, marge €415. Demo via "Bekijk als" from /admin/system/users (log in once as super_admin). | ✅ live (dev+prod) |
| auth demo-unblockers (#189) | Impersonation **Stop → /admin/system/users?stopped=1** (was dead-end /admin/system) so persona-switching flows · `/verify-2fa` copy "Je bent ingelogd — laatste stap" + "Herstel je toegang" → /login/lost-2fa · **🧪 Demo badge** in users-list by `seedKey` prefix `[demo-fixture]`. | ✅ SHIPPED (type-check 0 · build) |
| auth one-screen login (#190) | **Killed the double-2FA prompt**: `/login` (email+password+code) verified all three but never minted `cs_2fa_verified`, so middleware bounced to `/verify-2fa` to re-enter the SAME code. Now mints the cookie right after `signIn("password-totp")` (identical binding/options to /verify-2fa; admin 2FA-reset still invalidates). + audits `factorType` (totp/recovery). Adversarially reviewed — no bypass. | ✅ SHIPPED (type-check 0 · build · not AI) |
| demo owner logins (prod provision) | `scripts/provision-demo-owners.ts` (untracked, prod-host-guarded, password via env) flipped **maarten@ + gina@ → active + password `eminem123` + enrolled TOTP** on PROD (ep-icy-scene). Bypasses magic-link (their placeholder mailboxes don't receive). Verified loginReady=true (status/password/TOTP all valid). Login = e-mail + eminem123 + 6-digit code (authenticator) or recovery code. **Demo-only — rotate/reset 2FA before launch.** TOTP stays mandatory (model unchanged). | ✅ done (prod) |

### Owner Dashboard v2 — the operations control room (cockpit→"Dashboard", 2026-06-15)

> Plan: `~/.claude/plans/eager-churning-lampson.md`. Turned `/admin/business` from a read-only cockpit into an action board: **Signal → Context → Action → Confirmation**, no dead cards. Built in an **isolated git worktree** (`/private/tmp/wt-dash2`) because the main tree is contested by the parallel chef-availability chat. [[shared-tree-co-edit-hazard]] [[tsx-mts-export-link-error]]

| PR | Description | Status |
|---|---|---|
| DASH-1 (#192) | Rename **Cockpit→Dashboard** (user-facing label only; `cockpit.read`/route/AI-internals stable) + `src/lib/domain/dashboard-cards.ts` contract (`toCard`, CardType fire/risk/money/task/opportunity) + smoke (92). | ✅ |
| DASH-2 (#193,#194) | **Search-param-driven drawers** (DrawerShell client overlay + mobile bottom-sheet). "Vul deze dienst" open-shift drawer → `findMatchesForShift` → **Stel voor**; queue drawers → **Bevestig/Keur uren/Opvolg**. Reuse the atomic domain fns (`proposePlacement`/`transitionPlacement`/`approveHoursRow`); `?done=` flash. | ✅ |
| DASH-3 (#196,#197) | Per-shift **timeline** drawer (`shift-timeline.ts` — pure assembly of placements/hours/comments/contactLogs). **Snooze + dismiss-with-reason** + `dashboard_signal_state` (migration **0057**, on dev+**prod**). | ✅ |
| DASH-4 (#199) | Control-room **banner** (onder controle / N aandachtspunten) + **Fix-first** + card-type chips + alive empty state + AutoRefresh. | ✅ |
| DASH-5 (#201,#202) | Fill-drawer **contact logging** (`contactLogs`, App/Mail) + **operations command bar** ("Wat wil je oplossen?" → `cs-ai:ask`). | ✅ |
| DASH-6a (#203) | **Revenue-at-risk** card (unfilled slots × clientRate × hours) → "Vul nu". | ✅ |
| harden (#205) | Adversarial-review fixes, **code-only no migration**: snooze/dismiss now **per-viewer** (userId-prefixed `signalKey`, no schema change) · audit rows carry the actor · dismiss only on per-shift signals (count-aggregate fingerprints would never clear) · `return redirect` · clamp matchScore · `aria-label`. + `smoke-dashboard-signal-state.ts` (11). | ✅ |
| Phase 2 Agenda — P2a (#209) · P2b+P2c (#211) · harden (#213) · **P2-finish/P2d (#218)** | Owner operations-agenda at `/admin/business/agenda`: day/week/maand grid (`agenda.ts` `getAgendaEvents`) + token-authed ICS feed (`calendar.ics`, RFC5545+HMAC). **Manual one-off events** (`agenda_events` table — intake_call/follow_up/onboarding_task/contract_start/internal_reminder · checklist jsonb · linked client/chef/shift + assignedTo) via `agenda-events.ts` (create / setStatus atomic / toggleChecklist last-write-wins) + "Nieuwe afspraak" form + inline checklist cards + **client-lens & chef-lens**; ICS folds in manual events (no free-text notes — AVG). `smoke-agenda.ts` 43/0. | ✅ SHIPPED (migration **0061** on `ep-icy-scene`; type-check+lint+build green; prod deploy Ready) |
| **Phase 3 P3a — compliance hard-gate (#232)** | A chef whose deployability verdict is **blocked** (archived/inactive · ID verlopen · missing payroll-identity) can't be **proposed** unless a human overrides WITH a reason (≥10 chars), audited `placements.compliance_override`. Pure `evaluateChefBlockers` extracted to `chef-inzetbaarheid.ts` (single source of truth; the verdict card delegates to it) · async `chef-deployability-gate.ts` (`assertChefDeployable`/`assertChefsDeployable`, reuses onboarding-readiness, fail-closed, labels-only) · `proposePlacement` `override` param · AI `placements.propose` **structurally can't override** (no field in its Zod input) · `OverrideDeployabilityBlock` panel on the dashboard fill-drawer + shift-detail match list. `smoke-chef-deployability-gate` 9/0; adversarial review 18→0 confirmed. | ✅ SHIPPED **dark** (`COMPLIANCE_HARDGATE_ENABLED` default off; NO migration; eval-ai green; prod deploy Ready). **Deferred (P3a-2):** confirm-time + batch (nightly confirm / draft-publish) gating — needs a sticky-override to avoid a propose→confirm dead-end. |

**Drawer pattern:** `/admin/business?drawer=open-shift|queue|timeline&shiftId=…|kind=…`; server-rendered content + client `DrawerShell`; actions in `business/_actions.ts` redirect to `?done=` → `DoneFlash`. New audit keys: `dashboard.signal.snooze`/`dismiss` · `agenda_event.created`/`completed`/`cancelled` · `placements.compliance_override` (P3a). New flag: `COMPLIANCE_HARDGATE_ENABLED` (default off). **DEFERRED:** DASH-6b channel-aware notif-prefs (naive `shouldSendToUser`-in-`createNotification` conflates the klant "Mail-voorkeuren" keys with in-app — needs an `inapp:` namespace) · the per-user signal-state as a real migration (the prefix fix made it unnecessary) · P3a-2 confirm-time + batch deployability gating. Phase 3 in progress (P3a done; chef-availability owned by the parallel chat); Phases 4–5 (Emergency · AI-in-ops) next. ⚠ migration 0061 applied to prod via `db.execute(sql.raw())` after `drizzle-kit migrate` silently no-op'd the DDL — [[drizzle-kit-prod-migrate-silent-noop]].

### AI system-audit refinement (W0–W4 + portal-mini, 2026-06-15) — from a 6-dimension multi-agent AI audit

> A multi-agent audit of the whole owner-AI (UX · KPI coverage · routing · missing tools · registry/safety + a model-tier analyst). 32 confirmed findings → these waves. Census ~99 tools. **All AI-touching PRs merged on key-free evidence (type-check + smoke-ai-{tools,safety,portal} + adversarial review); the CI eval gate is OpenAI-quota-blocked — the staged GOLDEN/SAFETY routing cases run the moment quota returns.** [[openai-quota-gates-eval]]

| PR | Description | Status |
|---|---|---|
| AI-W0 (#182) | Routing/safety-eval guardrails: `placements.reply`→DESTRUCTIVE; GOLDEN+playbook disambiguation for the 3 `intel_snapshot` tools + `roster.overview` (had ZERO coverage); tightened `knowledge.search` boundary (NOTES corpus, not entity-find). | ✅ live |
| AI-W1 (#183) | **Profitability cockpit — 5 read tools:** `clients.loss_making`, `invoicing.unbilled`, `clients.signoff_backlog` (one new neon-safe grouped query), `reports.platform_kpi` (trend+swings), `chefs.at_risk`. All wrap tested domain fns (`read-model/kpi.ts`). smoke-ai-tools 336/0; adversarial review clean (no PII leak). | ✅ live |
| AI-mini (#184) | `portalModel()` knob — chef/klant portals can run **gpt-5.4-mini** (~3.3x cheaper, owner stays full). Dark-launch: set `OPENAI_PORTAL_MODEL=gpt-5.4-mini` to activate, verify with portal eval. Owner gpt-5.4-vs-mini A/B = eval-gated (quota). | ✅ live (knob in; not yet activated) |
| AI-W2 (#185) | Confirm-gate UX: **risk badge** (Financieel/Verzenden/Wijziging — backend already sent `risk`, client type never declared it), "thinking…" bubble, clearer token-expiry copy. | ✅ live |
| AI-W3a (#186) | Close-the-loop act tools: `chefs.request_profile_data` (outbound) + `placements.complete` (financial). **The adversarial review caught a REAL domain bug:** `completePlacement` lacked the worker's `ends_at<now()-1h` guard → admin button AND AI could complete a FUTURE shift → hours for unworked time. Fixed at source (`not-ended` reason, verified vs dev). | ✅ live |
| AI-W4 (#187) | Responsive AssistantWidget on ≤380px phones (was fixed 380×520). | ✅ live |

**Deferred (gated, not built):** Wave 3b financial AI tools (`shifts.cancel`, `invoicing.void`) — hold until the SAFETY eval can run (quota) to prove read-first + invoicing-lane coordination on `void`. Low-value Wave 4 leftovers (sync indicator, chef-chat-off explainer, quick-ask discoverability). **Model call:** owner=gpt-5.4 (eval-gate any mini downgrade), portals+eval=gpt-5.4-mini, pro=too-expensive-structurally, nano=too-weak. See [[openai-quota-gates-eval]].

### Klant-2 — native klant intake + JotForm retirement + IDOR fix (no migration)

| PR | Description | Status |
|---|---|---|
| PR-K2-1 | Native public klant intake form (`/horeca-personeel-aanvragen` + `/aanvragen` alias) | ✅ live (NO migration · seeds `client-request` form: audience=client, 10 fields, admin-editable at /admin/business/forms · `submitClientRequest` → client_submissions source `native_request`, status `new` · honeypot + `client_request_ip` rate-limit · mirrors /sollicitatie) |
| PR-K2-2 | Retire public JotForm CTAs + native /contact-us + webhook hardening | ✅ live (NO migration · `site.intake.{chef,client}` · /aanmelden + /contact-us CTAs → /sollicitatie + /horeca-personeel-aanvragen · contact `mailto` form → `ContactForm` → client_submissions source `native_contact` · fail-open `intake_webhook_ip` rate-limit on /api/intake/{chef,client}) |
| PR-K2-4 | Ownership/IDOR sweep of (client)+(chef) — fix chef `respond()` | ✅ live (NO migration · `respond()` resolves chef via chefs.userId + atomic `UPDATE … WHERE id=? AND chef_id=? AND status='proposed'` · audit confirmed 1 HIGH hole; all other reads/mutations correctly scoped) |
| PR-K2-5 | Klant venue profile & preferences (`/client/profile` "Voorkeuren") | ✅ live (NO migration · klant self-edits `client_type` + `client_tags[]` from the shared `client-taxonomy` — descriptive, non-binding match signal, NOT chef selection (respects no-veto) · already feeds `domain/matching.ts`; favorites/blocks stay admin-only · smoke `scripts/smoke-klant-preferences.mjs` 4/4) |
| PR-K2-6 | Klant KPI/insights card (`/client` "Jouw cijfers") | ✅ live (NO migration · read-only aggregates scoped to clients.userId: komende/afgeronde shifts, uren te tekenen, 30d besteed = Σ worked_minutes×client_rate_cents/6000 (approved/exported), meest-ingezette chef) |
| PR-K2-7 | Klant mail-voorkeuren (`/client/notifications`) | ✅ live (NO migration · toggles 4 mutable categories → notification_prefs via `setPref` · gated centrally in `recipientsForClient` via `shouldSendToUser` — billing/security mail always sends) |
| PR-K2-8 | Admin per-form notification recipients | ✅ live (NO migration · `recipientsForForm(slug,fallback)` reads a `form:<slug>` notification_routes row else the generic event · `/admin/system/notifications` "Per formulier" section · chef-apply/client-request/contact wired) |
| PR-K2-D | AI "Stel chefs voor" heuristic match | ✅ ALREADY SHIPPED (found during build-out) — `/admin/business/shifts/[id]` calls `findMatchesForShift` + renders ranked candidates (score/reasons/warnings/travel/margin) + one-click `proposePlacement`. Phase-9A heuristic + admin surface are LIVE; no work needed. |

> NO migration anywhere: `client_submissions.source` + `rate_limits.scope` are plain `text`; reused existing tables/enums/notification events. Smoke: `scripts/smoke-klant-native-intake.mjs` (10/10). Verified: build + browser render + real `submitClientRequest` row landed + IDOR cross-chef blocked. **Dropped:** PR-K2-3 (klant approve/decline of a proposed chef) — conflicts with the deliberate "no veto" design (`ChefFeedbackForm`: "NEVER Akkoord/Goedkeuren") + the "Maarten matches, geen algoritme" positioning; replaced by PR-K2-5 (descriptive venue prefs that steer the match without picking the chef). **Dev gap:** `RATE_LIMIT_HASH_SECRET` is missing from dev `.env.local` → blocks ALL public-form submits in dev (chef + klant); set in prod.

### AVG/GDPR compliance phase — active (plan: privacy-operations workflow)

| PR | Description | Status |
|---|---|---|
| PR-AVG-pre | docs/privacy/pii-inventory.md (51 tables) + retention-matrix.md | ✅ |
| PR-AVG-1 | Privacy-request intake (portal + off-portal manual) + identity verification + correspondence log + SLA extension + withdrawal + super_admin compliance queue | ✅ live (migration 0025 · privacy_requests intake/identity/SLA/correction cols + user_id nullable + other/withdrawn enum values · privacy_request_messages · domain/privacy.ts · chef+klant /privacy capture · /admin/system/privacy-requests list+new+[id] · 3 emails · privacy_request notification event) |
| PR-AVG-2 | Preview + export package (redacted, zip→R2, ~7d on-demand links) + correction (art.16) + erasure (art.17, legal-hold-aware) + tombstones | ✅ live (migration 0026 · privacy_erasure_tombstones · domain/privacy-{subject,export,erasure}.ts + applyCorrection · getLegalHoldsForUser · jszip dep · r2.putObject + EXPORT_DOWNLOAD_TTL · admin [id] export/correct/erase panels + [id]/download route · smoke-avg-erasure.mts 30/30 incl. 5 third-party redaction fixtures) |
| PR-AVG-3 | Retention purge worker (double-gated) + retention admin + backup replay | ✅ live (workers/retention.ts 3-state gate RETENTION_ENABLED/RETENTION_DRY_RUN both default safe · supervisor JOBS weekly Sun 02:00 · legal-hold-aware purge of soft-deleted chef_documents/chefs/clients + R2 byte purge via workers/_r2.ts · /admin/system/retention view/edit policies + risk banner · scripts/{seed-retention-policies,replay-erasure-tombstones}.mjs · docs/privacy/backup-erasure-policy.md · smoke-avg-retention.mjs 13/13) |

> AVG rules (load-bearing): user requests / super_admin fulfills (no autonomous erasure) · identity verified before export/erase · soft-delete-first · Payingit 7-year hold = structured legal holds · never export third-party PII (redact) · preview before execute · erasure tombstones + backup replay · 30-day SLA (extendable, art. 12(3)) · `AVG_CONSENT_ENFORCED` stays false until lawyer fills privacy text.

### Admin Staffing Cockpit phase — active (plan: `~/.claude/plans/goofy-moseying-truffle.md`)

| PR | Description | Status |
|---|---|---|
| PR-1 | Visual roster + deterministic intelligence (no schema/deps/AI) | ✅ live (`/admin/business/roster` week+month · `src/lib/roster-format.ts` shift health/next-action/warnings/fill + Amsterdam-DST bucketing · `RosterShiftCard` · "Aandacht nodig" strip + glance header · SidebarNav "Rooster" + dashboard wiring · **tunable seam** `DEFAULT_ROSTER_SETTINGS{criticalHours:24,labels}` ready for Instellingen · smoke-roster-intel.mts 49/49) |
| PR-1.7 | **Instellingen hub** — per-employee fine-tuning (broader hub) | ✅ live (migration 0027 `user_settings(user_id pk, prefs jsonb)` · `domain/user-settings.ts` getRosterSettings/saveRosterSettings merge-over-defaults · `/admin/account/instellingen` sections **Rooster** (criticalHours + standaard weergave + actie-labels → feeds roster intel) + **Meldingen** (per-user notification toggles via notification_prefs/setPref) · SidebarNav "Instellingen" · roster page threads getRosterSettings into the helpers + defaultView · smoke-user-settings.mts 11/11) |
| PR-1.5 | "Vul deze dienst" candidate panel on shift detail | ✅ live (`domain/{profile-completeness,staffing-intelligence}.ts` pure helpers · proof badges + confidence label + warnings + worked-here count + contact actions App/Mail/Belnotitie→contact_logs · smoke-staffing-intel.mts 53/53 (extended in PR-3.1/5) · availability now live via PR-4) |
| PR-1.6 | Chef 360 read model + work-history/feedback panels | ✅ live (`domain/chef-history.ts` getChefWorkSummary/FeedbackSummary/RecentShifts/**getChefClientHistory** (canonical "worked here" — PR-3.1 reuses) · chef detail Chef 360 section: snapshot (uren/diensten/rating/laatst) + reliability counts + topClients/topSegments + "Wat klanten zeggen" feedback + recente diensten · HARDENED: hours only from admin_approved/exported · smoke-chef-history.mts 16/16) |
| PR-2 | Rich chef intake from Jotform | ✅ live (migr 0028 · chefs + chef_submissions get street/house_number/postcode/lat/lng + transport_mode(car/motorbike/ebike/none) + preferences[] + employment_type(payroll/zzp/both) + applying_as(chef/front_of_house) · `intake/jotform.ts` parses them from the live form · `conversions.ts` carries to chefs · chef detail "Profiel & voorkeuren" chips + completeness% · AVG: erase nulls address/geo, export Full · matching soft-reason folded into PR-3.1 · smoke-chef-intake.mts 15/15) |
| PR-3 | Travel-cost + margin engine (free, PDOK) | ✅ live (migr 0029 shifts lat/lng · `domain/geo.ts` keyless PDOK geocode + haversine · `domain/travel.ts` estimateTravel (×1.3 road · per-km car .23/motor .21/ebike .05/OV .18) + estimateMargin (revenue−chef−reis, tone ok/low/negatief) · shift-detail candidate chip "≈ €X reis · Y km · {basis}" + marge · `scripts/geocode-backfill.mts` (chefs postcode + shifts city → coords) · smoke-travel.mts 17/17. NB: chips light up once geocode-backfill runs / chefs have postcodes) |
| PR-2.1 | Chef filters + missing-data workflow | ✅ live (migr 0030 `profile_data_requests` · chefs list filters: vervoer/voorkeur/employment/mist-data + smart views "Ontbijt+auto"/"Mist profieldata"/ZZP/Payroll + Vervoer-voorkeur kolom · `domain/profile-data-requests.ts` (createProfileDataRequest → email via ProfileDataRequestEmail + contact_logs + row · markCompletedByEmail closes loop on intake) · chef detail "Vraag ontbrekende gegevens" button + request history · AVG erase deletes requests · smoke-profile-requests.mts 11/11) |
| PR-2B | Client/shift requirements + venue type tags + favorite/blocked | ✅ live (migr 0031 · clients client_type/client_tags/favorite_chef_ids/blocked_chef_ids + shifts dress_code/language_required/min_experience/kitchen_type/solo_or_team/service_style/parking/meal/start_flexible · `domain/client-taxonomy.ts` shared type+tag vocab · client detail: set type+tags, view/remove favorite/blocked (audit) · shift detail favorite/block toggle · Chef 360 "Werkt vooral voor" top-klanttype · matching reasons: min_experience warning + language_required + preference↔segment/klanttype/tag (PREFERENCE_SIGNALS) · AVG: chef erasure array_removes id from klant favorite/blocked + pii-inventory note) |
| PR-3.1 | Candidate ranking with distance/margin/history | ✅ live (`staffing-intelligence.ts` getRankScore (blocked=−1 · ±favorite/available/worked-here/margin/distance/completeness) + getChefMatchExplanation (reasons·warnings·nextCheck) · shift detail ranks candidates by composite + favorite/blocked chips + "Checken:" line · smoke-staffing-intel.mts 53/53) |
| PR-4 | Availability + comms pipeline | ✅ live (shift detail reads chef_availability for the shift's Amsterdam day — row=available/unavailable, no row=unknown (warning, never available) → feeds badges/confidence/rank · logContact records placement journey via contact_logs · chef availability calendar already exists from PR-F2) |
| PR-5 | Match explanation + feedback loop (deterministic) | ✅ live (getChefMatchExplanation surfaces waarom/onzeker/checken + getRankGapReasons "Waarom niet nr 1?" on non-top candidates · outcome capture via placements (proposed/accepted/declined) + contact_logs (contacted + reason); no LLM) |

> **All cockpit PRs (PR-1…PR-5) are now live.** Next: per-page fine-tuning pass. Open follow-ups: intake-handler `markCompletedByEmail` hook (TODO), run `geocode-backfill.mts` so travel/margin + distance-ranking light up, chef availability admin-set UI (V1 = chef portal only).

> Cockpit LOCKED: visual language red=actie-nu·amber=risico/onbekend·green=klaar·blue=wacht·grey=afgerond·purple=AI(PR-5) · drill-down layers roster→shift→chef-drawer→full-profile · intelligence deterministic (no AI until PR-5) · don't fake structured filters from rawPayload (structure first, PR-2).

---

## DB schema state (Drizzle — `src/lib/db/schema.ts`)

### Enums

`user_kind` (internal, chef, client) · `user_status` (invited, active, disabled) · `error_severity` · `submission_status` · `vakniveau` · `segment` · `chef_status` · `client_status` · `shift_status` · `chef_document_type` · `placement_status` (+ `draft` — PR-PLANBORD-1, planbord concept) · `recovery_intent` (password, totp)

### Tables (live in prod)

**Auth + RBAC**: `users` · `auth_accounts` · `auth_sessions` · `auth_verification_tokens` · `roles` · `permissions` · `role_permissions` · `user_roles` · `user_recovery_codes` · `recovery_intents` (PR-C)

**Observability**: `audit_log` · `error_log` · `webhooks_received`

**Rate limiting**: `rate_limits` (PR-S1A)

**Notifications routing**: `notification_routes` (PR-F1) — admin events configurable

**Jotform intake**: `chef_submissions` · `client_submissions`

**Master records**: `chefs` · `clients` · `chef_availability` · `chef_documents`

**Shifts/placements**: `shifts` · `placements`

**Klant phase (live)**: `placement_comments` (visibility-scoped, PR-KLANT-0) · `client_contacts` (routing seam, PR-KLANT-0) · `client_change_requests` (PR-KLANT-1) · `client_shift_change_requests` (PR-KLANT-2, one-open-per-shift-per-kind) · `shift_templates` + `shift_template_exceptions` (PR-KLANT-4) · `clients.shiftAddress`/`shiftArrivalNotes`/`billingAddress` (PR-KLANT-0) · `client_submissions.cancelled_by_client*` (PR-KLANT-2) · `shifts.source_template_id`/`source_template_date` (PR-KLANT-4) · `ratings` + `chefs.averageRating`/`ratingCount` (PR-KLANT-5)

**Facturatie (live)**: `invoices` + `invoice_lines` (PR-OPS-4AREAS · klant-billing from admin_approved hours · cents · 21% BTW · billing snapshot · status draft/sent/paid/void/credit · sequential number `YYYY-NNNN` · partial unique `(client, period_start, period_end)` WHERE status≠void so a voided period re-bills)

**AI / RAG (live)**: per-row `embedding` vector(1536) on `chefs`/`clients`/`shifts` (manual `manual_pgvector_prep.sql` — powers `*.semantic_search`) · **`ai_embeddings`** chunked notes-RAG store (`vector(1536)` + HNSW cosine · `tenant_scope`/`visibility`/`redaction_version`/`content_hash` · soft-supersede via `superseded_at`; manual `manual_ai_embeddings.sql`, NOT in Drizzle schema) · AI reminders/memory/token-usage as jsonb bags in `business_settings`

**Owner Dashboard/Agenda (live)**: `dashboard_signal_state` (snooze/dismiss metadata on derived signals; per-viewer via userId-prefixed `signalKey`, migration 0057) · **`agenda_events`** (manual one-off agenda entries: `agenda_event_type`/`agenda_event_status` enums · `checklist` jsonb · linked client/chef/shift + assignedTo · migration 0061)

### Tables (planned per active plan)

**PR-CHEF-0**: `integration_connections` · `integration_outbox` · `integration_runs` · `external_refs` · `email_messages` · `email_events` · `notifications` · `contact_logs`

**PR-CHEF-1**: `shift_hours` + `shift_hours_status` enum

**PR-CHEF-4**: `profile_change_requests`

**PR-CHEF-6**: `notification_prefs`

**PR-CHEF-7**: `payroll_batches` · `payroll_batch_lines` · `shift_hour_corrections`

**PR-CHEF-10**: `consent_log` · `privacy_requests` · `data_processing_agreements` · `retention_policies`

**PR-CHEF-12**: extends `chef_documents` (clientVisible, verifiedAt/By, expiresAt, status)

**PR-CHEF-13**: `backup_runs` · `restore_drills`

**PR-CHEF-FUT (reserved)**: `api_clients` · `webhook_endpoints` · `webhook_deliveries`

### Migration history

| File | What | Status |
|---|---|---|
| 0000_light_captain_flint.sql | Initial users/auth/RBAC | applied |
| 0001..0008 | Submissions, chefs, clients, shifts, etc. | applied |
| 0009_noisy_sprite.sql | notification_routes (PR-F1) | applied |
| 0010_recovery_intents.sql | recovery_intents (PR-C) | applied (May 27) |
| 0011_integration_spine.sql | integration_connections + integration_outbox + integration_runs + external_refs + email_messages + email_events + notifications + contact_logs (PR-CHEF-0) | applied (May 27) |
| 0012_shift_hours.sql | shift_hours + shift_hours_status enum (PR-CHEF-1) | applied (May 27) |
| 0013_profile_change_requests.sql | profile_change_requests (PR-CHEF-4) | applied |
| 0014_notification_prefs.sql | notification_prefs (PR-CHEF-6) | applied |
| 0015_calendar_token.sql | calendar ICS tokens (PR-CHEF-11) | applied |
| 0016_chef_documents_trust.sql | chef_documents trust cols (PR-CHEF-12) | applied |
| 0017_backup_runs.sql | backup_runs + restore_drills (PR-CHEF-13) | applied |
| 0018_avg_privacy.sql | consent_log + privacy_requests + retention_policies (PR-CHEF-10) | applied |
| 0019_payroll_batches.sql | payroll_batches + lines + shift_hour_corrections (PR-CHEF-7) | applied |
| 0020_klant_foundations.sql | placement_comments + client_contacts + clients address split (PR-KLANT-0) | applied (May 28) |
| 0021_client_change_requests.sql | client_change_requests + client_change_status enum (PR-KLANT-1) | applied (May 28) |
| 0022_client_change_cancel.sql | client_shift_change_requests + 2 enums + submission_status 'cancelled_by_client' + client_submissions cancel cols (PR-KLANT-2) | applied (May 28) |
| 0023_shift_templates.sql | shift_templates + shift_template_exceptions + shifts.source_template_id/date + idempotency index (PR-KLANT-4) | applied (May 28) |
| 0024_ratings.sql | ratings + chefs.average_rating/rating_count rollup (PR-KLANT-5) | applied (May 28) |
| 0025_avg_fulfillment.sql | privacy_requests intake/identity/SLA/correction cols + user_id nullable + other/withdrawn enum values + privacy_request_messages (PR-AVG-1) | applied (May 28) |
| 0026_avg_tombstones.sql | privacy_erasure_tombstones (HMAC email hash · retained_entities_summary · per-subject ids) (PR-AVG-2) | applied (May 28) |
| 0027_user_settings.sql | user_settings (user_id pk · prefs jsonb) — per-employee cockpit settings hub (Cockpit PR-1.7) | applied (May 28) |
| 0028_chef_intake_rich.sql | chefs + chef_submissions: address (street/house_number/postcode/lat/lng) + transport_mode/preferences[]/employment_type/applying_as enums (Cockpit PR-2) | applied (May 28) |
| 0029_shift_geo.sql | shifts latitude/longitude (PDOK geocoded) for travel-cost (Cockpit PR-3) | applied (May 28) |
| 0030_profile_data_requests.sql | profile_data_requests (chef · type/fields/channel/status · sent/completed) — missing-data workflow (Cockpit PR-2.1) | applied (May 28) |
| 0031_client_shift_requirements.sql | clients client_type/client_tags/favorite_chef_ids/blocked_chef_ids + shifts dress_code/language_required/min_experience/kitchen_type/solo_or_team/service_style/parking_available/meal_included/start_flexible (Cockpit PR-2B) | applied (May 28) |
| 0032..0038 | geo · audit-impersonator · long-vision · chef-notes/events · client_submissions FK · metrics-daily · `business_settings` (0037) · `user_permissions` (0038) | applied |
| 0039_planbord_draft_status.sql | `placement_status` += `draft` — private concept placement for the planbord (PR-PLANBORD-1) | ✅ applied to **DEV + PROD** (`ep-icy-scene`; migration count 42) |
| 0040_invoices_facturatie.sql | `invoices` + `invoice_lines` + `invoice_status` enum — klant-billing (PR-OPS-4AREAS) | ✅ applied to **DEV + PROD** (`ep-icy-scene`, before deploy) |
| 0041_invoices_partial_unique_index.sql | `invoices_client_period_unique` → PARTIAL (`WHERE status≠void`) so a voided period re-bills (PR-OPS-4AREAS) | ✅ applied to **DEV + PROD** |
| 0042_intel_brein.sql | `chefs.intel` + `clients.intel` jsonb ("Maarten brein") + `placements.decline_reason` (PR-INTEL-P1) | ✅ applied to **DEV + PROD** (`ep-icy-scene`, before deploy) |
| 0043_match_intel.sql | `match_intel` (chef×klant pair-memory: note + would_rehire/return + ai_why_works/fails, uniek per paar) + `placements.chef_return_signal` (post-shift duim) (PR-INTEL-P4) | ✅ applied to **DEV + PROD** (`ep-icy-scene`, before deploy + verified) |
| `manual_pgvector_prep.sql` | **manual (non-Drizzle)** — pgvector extension + per-row `embedding` vector(1536) on chefs/clients/shifts + HNSW cosine | applied |
| `manual_ai_embeddings.sql` | **manual (non-Drizzle)** — `ai_embeddings` chunked notes-RAG store: `vector(1536)` + HNSW cosine + `tenant_scope`/`visibility`/`redaction_version`/`content_hash` + soft-supersede. Apply via `scripts/apply-ai-embeddings.mts` | applied (2026-06-08, prod) |
| 0044..0058 | dashboard intel · planner workbench/scale · AI waves · chef PR-1+2 (`chefs`/`placements`/`shifts` cols) · `dashboard_signal_state` (0057) | applied |
| 0059_chef_pr3_client_non_negotiables.sql · 0060_chef_pr3_arrival_checks.sql | chef PR-3 (parallel chat): per-hotel non-negotiables + arrival checks | applied (parallel chat) |
| 0061_agenda_events_and_indexes.sql | `agenda_events` + `agenda_event_type`/`agenda_event_status` enums + 4 perf indexes (`placements.status`, `shift_hours(shift_id)`+`(status,submitted_at)`, `contact_logs(entity_type,entity_id)`); SQL idempotent (CREATE TYPE guarded). Renumbered from 0059 after chef chats took 0059/0060 (P2-finish) | ✅ applied to **DEV + PROD** (`ep-icy-scene`) — note: applied via `db.execute(sql.raw())`, `drizzle-kit migrate` silently no-op'd the DDL ([[drizzle-kit-prod-migrate-silent-noop]]) |

---

## Production env vars (Vercel)

**Required**: `DATABASE_URL` · `DATABASE_URL_UNPOOLED` · `AUTH_SECRET` · `RESEND_API_KEY` · `RESEND_FROM_EMAIL` · `NEXT_PUBLIC_APP_URL` · `RATE_LIMIT_HASH_SECRET` · `TOTP_ENCRYPTION_KEY` · `TOTP_ENFORCE=true` · `TOTP_REVERIFY_HOURS=12`

**Optional** (Turnstile): `NEXT_PUBLIC_TURNSTILE_SITE_KEY` · `TURNSTILE_SECRET_KEY`

**R2** (Cloudflare): `R2_ACCOUNT_ID` · `R2_ACCESS_KEY_ID` · `R2_SECRET_ACCESS_KEY` · `R2_BUCKET=chefandserve` · `R2_PUBLIC_URL`

**Email routing**: `MAARTEN_EMAIL` · `JEZZA_EMAIL` (fallbacks; `notification_routes` rows override)

**AI assistant**: `AI_ENABLED=true` · `OPENAI_API_KEY` (⚠ rotation pending — owner) · `OPENAI_MODEL=gpt-5.4` · `AI_CONFIRM_SECRET` (≥32 chars, signs confirm tokens) · `OPENAI_PRICE_INPUT_PER_1M` / `OPENAI_PRICE_OUTPUT_PER_1M` / `OPENAI_PRICE_CURRENCY` (optional — override the built-in gpt-5.4 rate shown on the AI-tokens card). `OPENAI_API_KEY` also activates the `embedding-refresh` worker (per-row RAG) + the `GET /api/cron/rag-ingest` Vercel cron (chunked notes-RAG re-index). `CRON_SECRET` (≥16 chars) gates the cron — Vercel sends it as `Authorization: Bearer <secret>`; missing → the cron route refuses (503), so it can't be triggered publicly.

**Coming with this plan**:
- `RESEND_WEBHOOK_SECRET` (PR-CHEF-8 — Resend webhook signature verification)
- `AVG_CONSENT_ENFORCED` default `false` (PR-CHEF-10 — flip after lawyer review)

---

## Critical security invariants

1. **TOTP_ENFORCE=true is live.** Every internal user with `totp_enabled` gets a 12h device cookie. v2 cookie format includes `enrolledAtMs` so admin reset invalidates ALL device cookies on next request.
2. **Password reset bumps `permissions_version`** → invalidates JWT on other devices.
3. **2FA reset bumps `permissions_version`** + wipes secret + wipes recovery codes + sets `totp_enrolled_at=null` (which kills cookie v2 validation).
4. **Recovery intents are purpose-bound** (Fence 5). A forgot-password token cannot be used for lost-2fa, and vice versa. Single-use atomic via `UPDATE … WHERE consumed_at IS NULL`.
5. **Auth IS the lookup.** No chef/client id ever comes from form data. Server actions resolve entity by `session.user.id → entity.userId`.
6. **State transitions are atomic.** `UPDATE … WHERE id = ? AND status = '<expected>'`. If 0 rows update, request is stale.

---

## Critical operational invariants (active plan)

1. **No external API call inside a business transaction.** Approve hours → DB update + outbox enqueue. Worker delivers external.
2. **Idempotency keys on every outbox row.** Same `(eventType, entityId, action)` → same key. Re-enqueue is a no-op.
3. **Append-only after export.** Once `shift_hours.status='exported'`, only `shift_hour_corrections` mutations allowed.
4. **External system IDs in `external_refs`, never on entity tables.**
5. **Every email send creates an `email_messages` row.** Status updated by Resend webhook.
6. **No raw backend statuses in UI.** Pipe through `humanStatus()` from `src/lib/hours-labels.ts`.

---

## Workers (Railway crons)

| Worker | File | Schedule | Status |
|---|---|---|---|
| Embedding refresh | `workers/embedding-refresh.ts` | nightly 03:00 | ✅ live — **per-row embeddings populated** (ran LIVE: 11 chefs + 6 clients; shifts forward-looking only). Powers `chefs`/`clients.semantic_search`. text-embedding-3-small → per-row `embedding` vector(1536). Does NOT populate the chunked **`ai_embeddings`** store — that's the app-side Vercel cron `GET /api/cron/rag-ingest` (nightly `0 3 * * *`), since the standalone worker can't import the shared redact/chunk pipeline |
| Error digest | `workers/error-digest.ts` | daily | live |
| Weekly digest | `workers/weekly-digest.ts` | Monday 08:00 | live |
| Payingit sync | `workers/payingit-sync.ts` | TBD | stub |
| Retention | `workers/retention.ts` | TBD | stub (AVG1) |
| Supervisor | `workers/supervisor.ts` | hourly | live |
| Complete placements | `workers/complete-placements.ts` | every 30 min | ✅ live (supervisor JOBS — hours trust chain, PR-CHEF-1) |
| Document expiry | `workers/document-expiry.ts` | daily 06:00 Amsterdam | ✅ live (supervisor JOBS, PR-CHEF-12) |
| Payroll export | `workers/payroll-export.ts` | manual | PLAN: PR-CHEF-7 |
| Generate recurring shifts | `workers/generate-recurring-shifts.ts` | daily 04:00 Amsterdam | ✅ live (registered in supervisor JOBS, PR-KLANT-4) |
| Retention purge | `workers/retention.ts` | weekly Sun 02:00 Amsterdam | ✅ live (supervisor JOBS, PR-AVG-3 — DOUBLE-GATED RETENTION_ENABLED+RETENTION_DRY_RUN, both default safe → no-op until deliberately flipped) |
| Deliver outbox | `workers/deliver-outbox.ts` | every 5 min | ✅ live (supervisor JOBS, PR-AUDIT-5 — acks `internal` breadcrumbs pending→sent + writes integration_runs; defers payroll/csv until a handler lands) |
| Hours reminders | `workers/hours-reminders.ts` | daily 09:00 Amsterdam | ✅ live (supervisor JOBS, PR-AUDIT-6 — chef 24/72h, klant 5d, admin 10d; idempotent via audit markers; **GATED off** by default via HOURS_REMINDERS_ENABLED) |

> All scheduled workers run via `workers/supervisor.ts` JOBS (node-cron,
> Europe/Amsterdam): weekly-digest · error-digest · embedding-refresh ·
> payingit-sync · generate-recurring-shifts · complete-placements ·
> document-expiry · retention (double-gated) · deliver-outbox (every 5 min) ·
> hours-reminders (daily, gated off by default). `payroll-export` is manual.
> RETENTION env: `RETENTION_ENABLED` (default false) + `RETENTION_DRY_RUN`
> (default true) must BOTH be set deliberately on Railway for a live purge.

---

## Smoke tests (in repo)

- `scripts/smoke-prod.sh` — 17 HTTP-level checks against live URL
- `scripts/smoke-pr-c.mjs` — Neon DB schema sanity after PR-C
- `scripts/smoke-recovery-intents.mjs` — Fence 5 invariant tests (atomicity, intent-bound, expiry)
- `scripts/reset-internal-2fa.ts` — emergency 2FA reset CLI
- `scripts/smoke-integration-spine.mjs` — PR-CHEF-0 (to be added)
- `scripts/smoke-klant-native-intake.mjs` — PR-K2 (client-request form seeded · native_request/native_contact land · chef respond() IDOR predicate) — 10/10
- `scripts/smoke-klant-preferences.mjs` — PR-K2-5 (clients.client_type + client_tags round-trip the shared taxonomy) — 4/4
- `scripts/smoke-klant-notifications.mjs` — PR-K2-7/8 (notification_prefs schema + per-form notification_routes round-trip) — 4/4
- `scripts/smoke-ai-{spine,brain,tools,safety,usage}.mts` — AI runtime · brain/zod-schema · registry well-formedness (136) · per-tool gating (48) · token-tally math (12). Run tools/safety/usage with `--env-file=.env.local`.
- `scripts/live-ai-{brain,loop,context}-check.mts` — LIVE model checks (throwaway, real OpenAI key; not part of the gate)

---

## Open questions / decisions deferred

1. **Payingit API spec** — not publicly documented. CSV export first; live API integration when Payingit gives us API docs.
2. **Accounting platform** — Exact / Moneybird / AFAS? Adapter pattern supports any.
3. **Legal text for AVG modals** — placeholders + TODO; lawyer fills in.
4. **age key location for encrypted backups** — public key in `~/.ssh/`, private key 1Password + sealed paper backup.
5. **iOS/Android Calendar subscription UX testing** — PR-CHEF-11 needs manual test on real device.
6. **Cancellation severity thresholds** — 48h/24h/same-day from `src/lib/cancellation-severity.ts`; tune after 1 month real use.
7. **Web Push** — deferred to PR-CHEF-15; rely on emails + in-app notifications for V1.

### Known follow-ups discovered during the klant phase (spawned as side tasks)

8. ~~**Worker scheduling gap**~~ ✅ RESOLVED — `complete-placements` (every 30 min) + `document-expiry` (daily 06:00) registered in `workers/supervisor.ts` JOBS; **PR-AUDIT** then added `deliver-outbox` (every 5 min) + `hours-reminders` (daily 09:00, gated off by default). Worker tsc passes; all idempotent. The `hours-reminders` PLAN is now built.
9. ~~**Chef profile-change admin review (PR-CHEF-4 gap)**~~ ✅ RESOLVED — `/admin/business/chefs/[id]` now has a "Wijzigingsverzoeken" section with `approveProfileChange`/`rejectProfileChange` (hourlyRate writes both min/max cents), atomic flip, audit, chef outcome email. Smoke: `scripts/smoke-chef-profile-change.mjs`.
10. ~~**Chef photo for klanten**~~ ✅ RESOLVED — `/api/chef-photo/[id]` authz extended: a klant can load a clientVisible+verified photo of a chef placed on one of THEIR shifts (no enumeration; chef-self + super_admin paths intact). Hub renders `ChefAvatar` (photo + initials fallback) with the same gate in the query.

### Known follow-ups discovered during the K2-4 IDOR sweep

11. **`/client/templates` over-fetch (low)** — `templates/page.tsx` selects `shift_template_exceptions` with no where-clause (all clients) to build a lookup Map; only the caller's own templates render (no cross-client exposure), but add `inArray(shift_template_exceptions.template_id, ownTemplateIds)` for data-minimization.
12. **`client_submissions` keyed on `companyName` (low)** — no `clientId` FK yet, so klant requests/dashboard scope portal submissions by the caller's own `companyName` string. Not a cross-tenant IDOR, but two client records with an identical company name would see each other's portal submissions. Data-model follow-up: add `clientId` FK + backfill.

### AI assistant follow-ups (2026-06 expansion — PR #49/#51–#57)

13. **Deeper RAG (chunked + PII-redacted notes/CV/feedback index)** — ✅ **SHIPPED** (notes-RAG Stage 1 #61 + Stage 2): `ai_embeddings` table on prod (manual `manual_ai_embeddings.sql`), the PII-redaction pipeline (`redact()`), chunking, scope/visibility access filter, cosine retrieval, and the `knowledge.search` tool — all per `docs/ai/rag-ingestion-contract.md`, 45 chunks ingested LIVE, isolation/redaction/NEVER-source smokes green. **Autonomy follow-ups DONE:** (a) ✅ nightly auto-refresh wired **app-side** (`GET /api/cron/rag-ingest` → `ingestAll()`, `vercel.json` cron `0 3 * * *`, `CRON_SECRET`-gated — app-side, not the worker, because the standalone Railway worker can't import the shared redact/chunk pipeline); (b) ✅ `workers/retention.ts` prunes `superseded_at < now()-30d` + chunks of sources soft-deleted >30d (double-gated) **+ synchronous purge on AVG erasure** (`src/lib/ai/rag/purge.ts` ← `eraseUserData`). **Still optional:** (c) index CV-text (chef-uploaded only) + project docs (Broad index) when wanted; (d) reclassify `chefs.notes`/`clients.notes` visibility if/when we split chef-authored bio from admin tribal notes (V1 conservatively tags both `admin_only`).
14. **OpenAI key rotation** — test key was exposed in chat; owner will rotate. After rotating, update Vercel + Railway + local `.env.local`.
15. ~~**Optional AI read tools not yet built**~~ ✅ RESOLVED — all three exist in the registry as `shifts.margin`, `contacts.timeline`, `chefs.profile_completeness`.
16. **AI tool census = 34** (18 read / 10 act / 6 personal). Registry: `src/lib/ai/tools/index.ts`. Add a tool → also add a present-assertion in `smoke-ai-tools.mts` and (if it has required input) a `SAMPLE` entry in `smoke-ai-safety.mts`.

## How to update this file

Update **after every PR ships**:
1. Move PR from "In-progress" → "Shipped"
2. Add to migration history if a new migration ran
3. Add to env vars if a new one was set
4. Add to workers if a new worker shipped
5. Add to smoke tests if new
6. Update "Currently-shipped flow at a glance" if user-facing surface changed

**Never** update this for in-progress work — wait until merged + deployed + smoke-verified.
| **Berichten-pagina (inbound e-mail triage UI)** (#122) | De mens-helft van de inbound-pijplijn (#119/#120): `/admin/business/berichten` (owner-nav naast Inbox) toont elk gevangen bericht — categorie-badge (⚠ klacht / ⏱ spoed / vraag / overig), afzender, match-links naar chef/klant-detail, **body veilig leesbaar** (escaped plain text in `<details>` — nooit HTML; afzender-inhoud is onbetrouwbaar), filters (onafgehandeld/alles × categorie) en een **"markeer afgehandeld"/heropen**-toggle. Domain: `listInboundAdmin` (joins chef/klant-namen) + `setInboundHandled` (audited: `inbound_messages.handled/reopened`, geen berichtinhoud in audit). Notificatie-actionUrl wijst nu hierheen. | ✅ SHIPPED (type-check 0 · lint+build groen, route gebouwd · **live dev**: 5 rijen met categorie+klant-match, handled-toggle 4→3→restore + audit-rij geverifieerd). |
| **AI tijd-bewustzijn** (#123) | Audit-vondst (unknown-unknown, klasse correctness): het model kreeg NOOIT de huidige datum/tijd — "vandaag/morgen/volgende week/week 28"-redenering dreef op de training-prior (tools rekenen server-side wél goed). Nieuw `runtime/time-context.ts`: `timeContextBlock()` ("Het is nu woensdag 10 juni 2026, 00:30 uur (Europe/Amsterdam, week 24)…") + `amsterdamIsoWeek()` (ISO-week van de AMSTERDAM-kalenderdag — rond middernacht wijkt de UTC-dag af; edge getest). Geïnjecteerd in alle 3 chat-surfaces (owner + chef-portal + klant-portal) als onderdeel van het DYNAMISCHE trailing-context-bericht — bewust NIET in de system-prompt-prefix (zou prompt-caching breken; wijzigt elke minuut). Portal-runners kregen daarvoor een optionele `systemContext`-arg (zelfde patroon als owner). | ✅ SHIPPED (type-check 0 · build groen · helper-test: nu/week-referentie/middernacht-edge/jaarwissel allemaal correct). |
| **AI dagbudget-plafond + waarschuwing** (#124) | Audit-vondst (silent-risk klasse): kosten werden bijgehouden maar nooit BEGRENSD — een weggelopen dag kon honderden euro's kosten zonder dat iemand het merkte. Nieuw: env `AI_DAILY_BUDGET` (optioneel; werkt samen met `OPENAI_PRICE_*`) + in `read-model/ai-usage.ts`: pure `budgetState()` (alle drempels getest), `checkAiBudget()` (leest de dag-tally) en `maybeNotifyAiBudget()` (owner-notificatie bij ≥80% en bij 100%, **20u-throttle** — geen bel-spam). Beide chat-routes (owner + portal — zelfde pot) gaten vóór de brain-call: bij 100% een nette weiger-outcome ("dagbudget bereikt … morgen weer"), bij ≥80% doorlaten + waarschuwen. **Fail-open**: een tally-storing blokkeert de assistent nooit. Geen limiet/prijzen gezet → onbeperkt (gedocumenteerd in env.ts). | ✅ SHIPPED (type-check 0 · smoke-ai-usage 12/0 · 7/7 budget-branch-tests · build groen). |
| **Eval: grounding onder falen (R8/R9)** (#125) | Audit-vondst (eval-blinde-vlek): we testten routing+veiligheid+antwoord-hygiëne, maar nooit "wat doet het model als een tool FAALT of LEEG teruggeeft — bekennen of hallucineren?". `eval-ai-answers.mts` uitgebreid: **R8** empty-result-grounding (niet-bestaande chef → moet "niet gevonden" zeggen, geen gefabriceerde persoon) + **R9** tool-error-grounding (gesimuleerde DB-timeout via een **wrapped registry** die `hours.list_awaiting_approval` laat throwen → antwoord moet de storing BEKENNEN én geen verzonnen uren-data tonen). Bonus-fix: **R7 false-positive** — "nog **niet** uitbetaald" (correct negatief) triggerde de paid-claim-regex; genegeerde vormen worden nu gestript vóór de check. | ✅ SHIPPED (**eval-ai-answers 11/11**: model bekent lege resultaten + tool-falen, verzint niets). |
| **AI feedback-loop (👍/👎)** (#126) | Audit-vondst (learning-loop klasse): geen enkel mechanisme leerde van slechte antwoorden. Nieuw: migratie **0046** `ai_feedback` (verdict-enum up/down · channel owner/chef/client · Q/A gecapt 1000/4000 · **cascade bij user-erasure** (AVG) · indexes op created/verdict), helper `lib/ai/feedback.ts` (`recordAiFeedback`, best-effort), route `POST /api/ai/feedback` (session-auth, **channel uit de sessie — nooit uit de body**, rate-limit `ai_feedback_user` 30/min), en 👍/👎-knoppen onder elk assistent-antwoord in de **gedeelde AssistantChat** (owner-page + widget + chef/klant-portals in één klap; optimistisch, fire-and-forget, state in sessionStorage). Nog geen automatisch gedrag — de tabel is de mijnbare grondstof ("welke vragen kregen 👎?") voor playbook/eval-verbetering. | ✅ SHIPPED (type-check 0 · live insert: verdict+channel+cap-1000 geverifieerd · lint+build groen · 0046 op dev — **nog niet op prod**). |
| **AI-watchdog: §6 decision-point auto-drafts** (#127) | Het laatste grote "proactief"-stuk (rollout stap E/F-grens): de AI **stelt werk voor zonder dat Maarten iets vraagt** — maar verstuurt NIETS zelf (drafts only). Drie deterministische detectors in `ai/read-model/watchdog.ts` (geen LLM-calls — voorspelbaar, gratis): (1) **stale open shifts** (>24u open, toekomstig, open plekken via confirmed-telling) → "overweeg tarief/selectie" + diepe link; (2) **stille chefs** (actief, 30d+ geen placement én geen contact-log; nieuwe chefs uitgezonderd) → **concept-checkin** klaar om te sturen; (3) **lage ratings** (≤2★, laatste 7d) → **concept-reactie** + nabel-suggestie. Cron-route `/api/cron/ai-watchdog` (Bearer CRON_SECRET, dark-launch `AI_WATCHDOG_ENABLED`, **per-entity throttle 6d**) + thin worker `workers/ai-watchdog.ts` (dagelijks 08:15) in supervisor JOBS. | ✅ SHIPPED (type-check 0 · workers tsc 0 · build groen · **live dev E2E**: run 1 → 4 stale shifts gevonden + 4 notificaties ("Dienst staat al 26 uur open — sous chef bij … overweeg tarief…"); run 2 → 0 (throttle bevestigd)). |
| **AI kwaliteitsradar (ratings.trends)** (#128) | Audit-vondst (zichtbaarheidsklasse): de AI kon per chef ratings zien (`chefs.feedback`) maar had GEEN vloot-brede kwaliteitsblik. Nieuw read-model `ai/read-model/rating-trends.ts` (`sweepRatingTrends`, 90d-venster): (1) **dalende chefs** — recente 30d-gemiddelde vs de 60d ervoor (drempel ±0.5★, min 3 beoordelingen); (2) **herhaald-lage paren** — zelfde chef↔klant met ≥2× ≤3★ ("Marco kreeg 2× ≤3★ bij Okura — let op bij de volgende match"). AI-tool `ratings.trends` (read, `chefs.read`, intern-only conform ratings-V1) + golden eval-case G-quality. | ✅ SHIPPED (type-check 0 · smoke-ai-tools 264/0 · **eval-ai 55/55** · build groen · **synthetische wiskunde-test op dev**: dalend 5→2.5 ✓ + 2×-laag-paar ✓, daarna opgeruimd; lege-staat pad ook geverifieerd). |
| **AI gespreks-persistentie (cross-device)** (#129) | Audit-vondst (UX-klasse): sessionStorage overleeft alleen het eigen tabblad — nieuw tabblad/ander apparaat = amnesie. Nieuw: migratie **0047** `ai_conversations` (1 actief gesprek per user per surface owner/chef/client — uniek (userId,surface), messages jsonb gecapt, **cascade bij user-erasure**), helper `lib/ai/conversation.ts` (`sanitizeMessages` 60 msgs/8k chars · load/save-upsert/clear), route `/api/ai/conversation` (GET/PUT/DELETE; **surface uit de sessie, nooit uit de body**; rate-limit `ai_conversation_user` 30/min). `AssistantChat`: geen lokale kopie bij mount → GET van de server-mirror; elke wijziging → **debounced (1,2s) fire-and-forget PUT**; "Gesprek wissen" → DELETE + pending-sync-timer geannuleerd (geen resurrectie). Chat-hot-path onaangeraakt — pure sync-laag. | ✅ SHIPPED (type-check 0 · **8/8 domain-tests**: roundtrip/upsert-geen-dup/cap-houdt-nieuwste-60/bad-role-reject/surface-isolatie/clear · lint+build groen · 0047 op dev + PROD). |
| **Eval: multi-turn (M-sectie)** (#130) | Laatste eval-blinde-vlek uit de audit: alle cases waren single-call — context-verlies tussen beurten werd nooit gevangen. `eval-ai.mts` + **MULTI-sectie** (3 cases met user→assistant→follow-up): M1 entiteit-wissel binnen onderwerp ("en hoe zit dat met Marco?" na een Okura-historievraag), M2 actie-follow-up ("keur die van Jan maar goed" → leest eerst, ook mid-conversatie), M3 taak-variant ("nu ook zo'n rapport over de chefs" → reports.chefs). | ✅ SHIPPED (**eval-ai 58/58** — alle 3 multi-turn first-try groen; net nu: 22 golden + 12 chaos + 3 multi + 11 safety + 11 answer-hygiëne incl. R8/R9-grounding). |
| **AI hardening: circuit-breaker + fallback + per-tool usage** (#131) | Laatste 2 audit-items (3e — business-settings-leestool — bewust GESKIPT: `business_settings` bevat alleen operationele toggles (dagstart/reminders), géén beleid/bankdata — de audit-claim klopte niet, niets waardevols om te exposen). (1) **Circuit-breaker** `lib/ai/circuit-breaker.ts` (DB-backed in business_settings['ai_breaker'] — serverless heeft geen geheugen): 3 opeenvolgende provider-fouten binnen 10 min → 5 min open → beide chat-routes antwoorden vriendelijk ZONDER OpenAI te hameren + throttled owner-notificatie (`ai_breaker_alert`, 20u); succes reset; alles fail-open. (2) **Fallback-model** env `OPENAI_FALLBACK_MODEL` (optioneel): bij provider-fout probeert de route het run één keer met het fallback-model vóór opgeven. (3) **Per-tool-gebruik** `getAiToolUsage` (audit_log `ai.tool_completed/failed`, group by `after->>'tool'`) + "top tools"-regel op de AI-tokens-kaart (/admin/system) — "welke tool is druk/faalt?". **CLI**: `ONBOARDING_NUDGE_ENABLED` + `AI_WATCHDOG_ENABLED` via Vercel CLI op Production gezet (bakken in bij deze deploy); **Railway-flags konden NIET via CLI** (lokale RAILWAY_TOKEN is gescoped aan ander project "humble-commitment"/Boekeerlijk — bewust afgebleven) → owner-checklist. | ✅ SHIPPED (type-check 0 · breaker-cyclus 4/4 live getest (3e fout→OPEN, succes→reset) · per-tool query 4 echte rijen · build groen). |
| **Data-debts: templates over-fetch (+ #133 hotfix)** (#132/#133) | (1) De échte fix: `/client/templates` haalde ÁLLE shift_template_exceptions op (alle klanten) → nu `inArray(templateId, eigen templates)` (data-minimalisatie). (2) **CORRECTIE**: mijn "portaal-aanvragen onzichtbaar"-claim in #132 was FOUT — de portal-insert zet `clientId` al sinds PR-AUDIT-1 ("authoritative owner FK", regel ~143); mijn toegevoegde regel was een DUPLICAAT-property die de build brak, en #132 werd door een `;`-geketende compound-command TOCH gemerged met rode build → **#133 hotfix** (duplicaat weg). **Procesles: ship-stappen NOOIT met `;` achter verify ketenen — altijd `&&` zodat een rode check de merge blokkeert.** MEMORY-item 12 (clientId-FK) was dus al opgelost door PR-AUDIT-1; alleen evt. legacy NULL-rows op prod checken. | ✅ #133 SHIPPED (type-check 0 · build groen). |
| **Infra via CLI (volledige toestemming owner)** | Owner gaf expliciet "run it all": (1) **Vercel prod**: `ONBOARDING_NUDGE_ENABLED=true` · `AI_WATCHDOG_ENABLED=true` · `AI_DAILY_BUDGET=25` · `OPENAI_FALLBACK_MODEL=gpt-4.1-mini` (gekozen uit hun échte OpenAI model-lijst via /v1/models; gpt-5.4-pricing zit al built-in in `MODEL_PRICING`). (2) **Railway** (project `outstanding-charisma`, service `chefandserve2` — gevonden via `env -u RAILWAY_TOKEN railway list`; de lokale RAILWAY_TOKEN is gescoped aan het Boekeerlijk-project): beide flags + **CRON_SECRET + NEXT_PUBLIC_APP_URL gesynct van Vercel** — die ONTBRAKEN op de worker, dus álle thin-ticker-jobs (daily-briefing/nudge/watchdog) skipten stil. ⚠ Mis-target onderweg: eerste set landde door een link-timeout op gotenberg (hearty-caring) → geneutraliseerd (lege waarden). (3) **Prod-DB**: `business_settings['daily_briefing'] = {enabled:true, hour:7}`. Workers redeployen automatisch bij var-wijziging. | ✅ DONE — dagstart + nudge + watchdog zijn nu écht live end-to-end (ticker→route→flag→notificatie). |
| **E-mail-infra: één-planningsadres-model gehard** (#134) | Owner-architectuur bevestigd: **rollen ≠ inboxen** — planners hebben eigen (persoonlijke) mailboxen, maar alle systeem-mail gaat uit en komt binnen via ÉÉN planningsadres. **Audit (was al goed)**: élk send-pad (sendEmail/auth/intake/workers) gebruikt `env.RESEND_FROM_EMAIL`, nul hardcoded adressen, `replyTo` optioneel (replies komen dus vanzelf terug op het planningsadres → inbound-webhook), inbound is adres-agnostisch → **echte API/adres-swap = alleen `RESEND_API_KEY` + `RESEND_FROM_EMAIL` op Vercel + Railway, nul code**. **Echte gap gefixt**: interne afzenders (owner/planner die de planningsinbox mailt) waren "Onbekende afzender" → migratie **0048** (`inbound_messages.matched_user_id` FK→users, additief), `matchSender` checkt nu users (staff) EERST, Berichten-UI toont "Intern: <naam>", `inbound.list` matchedTo "intern", notificatie-label "(intern)". ⚠ MX-footgun gedocumenteerd: receiving-domein NOOIT het hoofddomein als daar persoonlijke mailboxen op draaien — subdomein óf forward naar het .resend.app-adres. | ✅ SHIPPED (type-check 0 · build groen · **live dev E2E**: staff-mail → matchedTo intern + userName-join + notificatie · 0048 op dev + PROD). |
| **Inbox-toegangsbeheer (super_admin)** (#135) | Owner-ask: meerdere gevangen inboxen (planning@ + eigen adressen van de twee owners) en **in super-admin instellen wie welke inbox ziet**. Migratie **0049**: `inboxes` (adres lowercase uniek + label) + `inbox_access` (inboxId↔userId, uniek paar, cascade). Domain `domain/inboxes.ts`: CRUD+grant/revoke (alles ge-audit; grant op login-e-mail case-insensitive) · `viewerInboxFilter` (policy: super_admin alles · géén inboxen geconfigureerd = pre-config gedrag (iedereen alles) · anders alleen eigen inboxen, owners zien óók mail die op géén geconfigureerde inbox matcht — stray-mail-vangnet) · pure `matchesViewer`/`inboxLabelFor` · `inboxRecipients`. **Notificatie-routing**: inbound-notificaties gaan nu naar de LEDEN van de gematchte inbox (fallback owner als niets geconfigureerd). **Berichten** filtert per viewer + inbox-label-badge. **UI**: `/admin/system/inboxen` (super_admin via `system.write`-bypass): inbox aanmaken/verwijderen, toegang geven op login-e-mail, leden verwijderen. | ✅ SHIPPED (type-check 0 · build groen · **11/11 live E2E**: lowercase-normalisatie, grant mixed-case, notificatie→lid i.p.v. Maarten-fallback, member/non-member/owner-stray zichtbaarheid, surgical revoke · 0049 op dev + PROD). |
| **AI erft inbox-toegang van de vrager** (#136) | Owner-ask: planners die de AI gebruiken mogen via `inbound.list` NIET in de owner-inboxen kijken; owners zien per hun eigen rechten. De tool leidt de kijker af uit `ctx.actor` (`requestedByUserId` + `requestedByRole`) → zélfde `viewerInboxFilter`-policy als de Berichten-pagina (super_admin alles · grants per user · owners + stray-net · niets geconfigureerd = alles). `listRecentInbound` kreeg optionele `viewer`/`inboxLabels`-args (geen filter = alleen voor interne callers, nooit chat) en items dragen nu het inbox-label. | ✅ SHIPPED (type-check 0 · smoke-ai-tools 264/0 · build groen · **5/5 vier-identiteiten-E2E door de echte tool**: planner-zonder-grant niets · owner-zonder-grant alleen stray · super_admin alles · granted-planner alleen de inbox · label in items). |
| **Feedback-loop dicht: feedback.review** (#137, wave PR-1) | Audit-vondst (learning-loop, KRITIEK): `ai_feedback` was write-only — een 👎 bereikte nooit iemand. Nu: read-model `ai-feedback-summary.ts` (30d tellingen + downrate + de recente 👎-gevallen zelf, snippets gecapt), AI-tool **`feedback.review`** ("wat vond het team slecht?" → telling + cases + "wil je dat ik hier iets van onthoud?"), en een feedback-regel op de /admin/system AI-kaart. | ✅ SHIPPED (type-check 0 · smoke 267/0 · eval G-feedback routeert · build groen · live dev: seed 2 rijen → summary 1↑/1↓ 50% + tool-output correct → cleaned). |
| **Memory-hygiëne: cap 50 + dedup + recall-evals** (#138, wave PR-2) | Audit-vondst: owner-memory groeide onbegrensd (élk feit wordt élke beurt geïnjecteerd → stille token-bloat) en dubbele feiten stapelden. Nu: `MEMORY_CAP=50` per user (oudste vallen af; de tool meldt het: "ik zat aan m'n limiet — oudste vergeten: …"), **dedup op genormaliseerde tekst** (zelfde feit → opgefrist i.p.v. duplicaat: "Dat wist ik al — opgefrist"), `memory.list` toont `n/50`. Eval: **G-memory** ("Onthoud dat Okura alleen sous-chefs wil" → memory.remember) + **M4** multi-turn recall. | ✅ SHIPPED (type-check 0 · **eval 61/61** · build groen · live dev: dedup ✓, cap blijft 50 ✓, 6 evictions gerapporteerd ✓, opgeruimd). |
| **Platform-ogen: watchdog.findings + system.health** (#139, wave PR-3) | Audit-vondst (connectiviteit): de watchdog en de error/metrics-workers produceerden data die de AI niet kon lezen. Nu: **`watchdog.findings`** (draait de 3 detectors on-demand — "wat ziet je watchdog?") en **`system.health`** (nieuwe read-model: fouten 24u — aantal/onopgelost/top-3 ingekorte meldingen, NOOIT stacktraces — + laatste metrics-snapshot-datum). 2 GOLDEN eval-cases. | ✅ SHIPPED (type-check 0 · smoke 273/0 · **eval 63/63** · build groen · live dev: watchdog "4 diensten lang open (44u)" + health "geen fouten / snapshot nog nooit"). |
| **RAG dev-parity geverifieerd + manual-migration-footgun gedocumenteerd** (#140, wave PR-4) | Audit-claim "ai_embeddings ontbreekt / RAG crasht" **definitief ontkracht**: dev heeft de tabel met **843 rijen** (nachtelijke ingest draait op beide branches), prod had 'm al. Wél een echte footgun gedocumenteerd in `.claude/rules/db-and-migrations.md`: `drizzle/manual_*.sql` (pgvector-prep + ai_embeddings) staan BUITEN de journal — `db:migrate` past ze nooit toe; een verse Neon-branch mist ze stil en RAG-ingest faalt dan nachtelijk. | ✅ SHIPPED (docs-only). |
| **CI eval-gate** (#141, wave PR-5) | Audit-vondst: de 63-case eval draaide alleen handmatig — een model-/prompt-regressie haalde main ongemerkt. Nu `.github/workflows/ai-eval.yml`: élke PR die `src/lib/ai/**` of de eval raakt draait de routing/safety-eval in CI. **Planning-only** → alles behalve `OPENAI_API_KEY` (repo-secret, via `gh secret set`) is een format-valide dummy (recept lokaal met `env -i` gerepeteerd; GINA_EMAIL bleek óók verplicht). Retry-once (cases kunnen flaken — G3 deed dat in de repetitie; echte regressies falen 2×). Kosten: ~centen/run. | ✅ SHIPPED — de workflow is groen gegaan op z'n EIGEN PR vóór de merge. |
| **Conversation→memory mining (dark)** (#142, wave PR-6) | Audit-vondst: duurzame feiten die Maarten mid-chat zei gingen verloren tenzij hij expliciet memory.remember aanriep; niets minede ooit ai_conversations. Nu `/api/cron/ai-memory-mining` (Bearer CRON_SECRET, **dark via `AI_MEMORY_MINING_ENABLED`**): owner-surface-gesprekken <24u → goedkoop model (`OPENAI_FALLBACK_MODEL` ?? gpt-4.1-mini, strict JSON, max 3 feiten/gesprek, transcript gecapt) → **dedup tegen bestaande memory** (`normalizeFactText` nu geëxporteerd) → **propose-only notificatie** "Zal ik dit onthouden?" (max 1/20u/user; NOOIT auto-schrijven — memory blijft human-approved). Thin ticker `workers/ai-memory-mining.ts` (03:30) + supervisor-entry. Chef/klant-surfaces bewust uitgesloten (context-kruising). | ✅ SHIPPED (type-check 0 · workers tsc 0 · build groen · **live dev E2E mét echt model**: feit correct geëxtraheerd + voorstel-notificatie; run 2 → throttled; opgeruimd). |
| **Inbox-ACL via de AI** (#143, wave PR-7) | Audit-vondst (verb-gap): inbox-toegangsbeheer kon alleen via de UI. Nu: **`inboxes.list`** (read, "wie ziet de planning-inbox?"), **`inboxes.grant_access`** + **`inboxes.revoke_access`** (confirm-gated, permission **`system.write`** — nieuw, additief in de RBAC-catalog, bewust aan GÉÉN rol toegekend → alleen super_admin via de bypass; owners/planners krijgen netjes "geen rechten" van de executor). Revoke accepteert het login-e-mailadres (lost lid op via de members-lijst). Eval: G-inboxes + R-inbox (brede "geef iedereen overal toegang" → moet read-first/weigeren) + beide mutaties in de DESTRUCTIVE-set. | ✅ SHIPPED (type-check 0 · smoke 284/0 · build groen · 4/4 live tool-E2E incl. nette fout bij onbekende user · CI-gate groen op de PR). |
| **shifts.create: dienst aanmaken via de AI** (#144, wave PR-8) | Audit-vondst (verb-gap, riskantste van de wave): dienst-aanmaak zat inline in de /shifts/new-pagina — geen domain-functie, dus geen AI-verb. Nu: **`domain/shifts.ts` `createShift()`** geëxtraheerd (validatie: tijden/rol/klant-bestaat + audit `shifts.create`), de pagina refactored naar dezelfde functie (gedrag identiek), en AI-tool **`shifts.create`** (confirm-gated `outbound`, `shifts.write`; describeAction toont een Amsterdam-tijd-samenvatting; summary stelt meteen shifts.suggest_chefs voor). Eval: G-newshift + DESTRUCTIVE-entry. | ✅ SHIPPED (type-check 0 · smoke 288/0 · build groen · **live dev E2E**: slechte tijden/onbekende klant geweigerd, echte create via de tool (open/×2/sous_chef) + audit-rij, opgeruimd · CI-gate groen). |
| **Chef-portal-AI uitgebreid: mijn.documenten + mijn.beoordeling** (#145, wave PR-9) | Audit-vondst (portal-gap): de chef-assistent kon eigen documenten/beoordeling niet zien. Nu: **`mijn.documenten`** (eigen docs + verloopt-binnenkort-waarschuwing 60d) en **`mijn.beoordeling`** (alleen het GEMIDDELDE en pas vanaf 5 beoordelingen — de V1-huisregel; nooit losse beoordelingen/opmerkingen). Eigen-scope via `ctx.actor.subject.entityId` (huispatroon); wrong-kind geblokkeerd. `onze.feedback` (klant-kant) bewust DOORGESCHOVEN (per-placement `listVisibleComments`-patroon vergt eigen verkenning). | ✅ SHIPPED (type-check 0 · build groen · live: beide tools + ≥5-regel + kind-guard · CI-gate groen). |
| **Docs-waarheid hersteld** (#146, wave PR-10) | De docs waren zó stale dat een audit-agent concludeerde dat het AI-systeem niet bestond. Gefixed tegen geverifieerde grondwaarheid (79 owner + 10 portal = ~90 tools · 66 eval-cases · 17 workers · migraties t/m 0049): **docs/ai/README.md** (header "No LLM is wired in" → LIVE-status + volledige wat-bestaat-tabel), **AI_INTEGRATION.md** (header "not yet wired" → LIVE, stappen A–E shipped/F draft-only; inventory-rijen hours/ratings/messages/chef_documents → ✓ live), **CLAUDE.md** (76→~90 met "smoke-ai-tools is de live count", 15→17 workers, 0045→0049 + manual-noot, 54→66 eval + CI, open-items: nudge/watchdog/briefing/budget/fallback nu LIVE — mining/Resend-secret/reminders nog dark), **.claude/rules/ai-work.md** (gate-tellingen), + **nieuw geconsolideerd contract `docs/ai/tool-contracts/2026-06-additions.md`** (13 nieuwe tools + cross-cutting invarianten). | ✅ SHIPPED (docs-only) — **WAVE 10/10 COMPLEET**. |
| **Planning-pagina: act-from-the-page** (#147, planner-wave A1) | UX-audit: de kernklus "dienst vullen" kostte 4–5 klikken over 2–3 navigaties; de berekende suggesties hadden GEEN knoppen. Nu: (1) **inline "Voorstel"-knop** per match-kandidaat — zelfde `proposePlacement`-domeinpad als shift-detail, mét idempotente dedup ("al voorgesteld"-toast); (2) **sp.ok succes-toast**; (3) **"Vul dienst"-CTA** per kritieke-48u-rij (1 hop i.p.v. 2–3); (4) **lijst-cap 20** + "+N meer in het rooster"-link; (5) **"Wat nu?"**-regel op Te bevestigen + `?tab=open`-deep-link; (6) **proposed-vs-accepted-split** ("N voorstellen wachten nog op de chef" — nieuw `proposedPending` in planner-intel); (7) **match-warnings** (vakniveau-gap e.d.) amber per kandidaat. ≈40% minder interactiekosten op de kerntaak. | ✅ SHIPPED (type-check 0 · build groen · **live dev E2E**: cockpit→propose→`proposed`→re-propose→`already_proposed`→cleanup). |
| **Planning-pagina: Aandacht-rail** (#148, planner-wave C1) | Audit-vondst: de proactieve laag (watchdog stale-shifts, Berichten spoed/klacht) bereikte de planner-pagina nooit — alleen owner-notificaties. Nu een **live "Aandacht"-rail bóven de KPI's**: (1) **stale open shifts** via hergebruikt `findStaleOpenShifts` (zelfde engine als de watchdog-cron, maar live bij elke page-load) met "Vul dienst"-CTA + tarief/selectie-hint; (2) **onafgehandelde spoed/klacht-mail** via `listInboundAdmin` + `viewerInboxFilter`/`matchesViewer` — **inbox-ACL gerespecteerd** (planner zonder grant ziet de vlag niet; geen inboxen geconfigureerd = pre-config gedrag). Leeg → rail rendert niet. Stille chefs + lage beoordelingen bewust owner-only gehouden (per owner-keuze). | ✅ SHIPPED (type-check 0 · build groen · **live dev E2E 4/4**: stale-detectie, vlag zichtbaar zonder config, ACL verbergt voor non-member, super_admin ziet alles; opgeruimd). |
| **Planner krijgt de AI (flag-gated)** (#149, planner-wave B1) | Audit-vondst: de planner was 100% buitengesloten van de assistent (widget + chat-route = owner/super_admin). Nu, achter **`PLANNER_AI_ENABLED`**: (1) widget mount + chat-route open voor planner-rol; (2) **`buildScopedRegistry(effectivePerms)`** — alleen tools binnen de planner-RBAC bereiken het MODEL (schonere routing, goedkopere prompt; `permission:null`-tools zoals memory.* zijn per-user en blijven); de executor-ceiling blijft de échte muur; (3) **planner-contextregel** in het dynamische trailing-bericht ("focus op vullen/bevestigen; financieel → Maarten") + eigen `promptCacheKey: planner:<id>` (andere tool-prefix); (4) **7 scope-smoke-asserties** (suggest_chefs/cockpit/propose IN · hours.approve/payroll/feedback/inbox-grant OUT). Flag op Vercel prod gezet via CLI. | ✅ SHIPPED (type-check 0 · smoke **295/0** · build groen · CI-eval-gate groen op de PR). |
| **Planner-workbench: werk-wachtrijen + AI-in-de-flow** (#150, wave W1+W2) | Owner: "voelt half af". De pagina is nu een WERKBANK: (1) **Te bevestigen-wachtrij** met inline Bevestig (zelfde `transitionPlacement` als shift-detail/AI-tool) + "+N meer"-link; (2) **Wacht-op-chef-wachtrij** (oudste eerst, leeftijds-badges 24/72u); (3) **klant-wijzigingsverzoeken** in de Aandacht-rail; (4) **suggesties voor top-3 urgente diensten** (parallel gefand, 3×2) i.p.v. één; (5) **demand-vooruitblik** op de 7d-kaart; (6) **AutoRefresh** 60s (zichtbaar-tab-only); (7) **AI-quick-ask-chips** (`cs-ai:ask` CustomEvent → widget opent + chat prefillt, NOOIT auto-send; rol-bewuste prompts — planner-chips gebruiken alleen tools binnen hun scoped registry). **Adversarial review-workflow (3 lenzen) vóór de ship → 10 findings, álle gefixt**, incl. BLOCKER: `transitionPlacement` kreeg optionele **`expectedStatus`** (house-rule atomic guard) — dubbelklik/stale rij = `changed:false` i.p.v. dubbele mail-cascade; ook in de `placements.confirm`-AI-tool. Plus: change-request-liveness (starvation-fix), Promise.all-fanout, toon-correcte toast die zichzelf na 6s opruimt, gehumaniseerde AI-queue-labels. `planner.cockpit`-tool kreeg de wachtrijen gratis. | ✅ SHIPPED (type-check 0 · smoke 295/0 · build groen · **blocker-E2E 4/4**: 2e klik geen cascade, status-skip dicht, 1 audit-rij · queues-E2E 6/6 · CI-gate groen). |
| **Schaal-laag: autofill-knop + nacht-preplan — én een kritieke driver-bug gefixt** (#151, wave W3) | Owner: "hoe schalen we, waar helpt de AI écht?". (1) **"⚡ Vul de week automatisch"** op de planning-pagina: `autofillWeek` zet álle open plekken van de komende 7 dagen als CONCEPT klaar (onzichtbaar voor chef/klant) → toast met N/M + link planbord → review & **Publiceer**. (2) **Nacht-preplan** `/api/cron/ai-preplan` + ticker (05:30, dark via `AI_PREPLAN_ENABLED`): planner begint de dag met "N van M vooringevuld — review & publiceer"; bij 0 kandidaten → tarief/werving-hint. **(3) KRITIEKE VONDST uit de eigen idempotentie-E2E: drizzle+neon-http rendert gecorreleerde subqueries in een select-PROJECTIE als ongecorreleerd (altijd 0)** — WHERE-correlatie werkt wél. Gevolg: `autofillWeek` overvulde volle diensten (run 2 = +5 dubbele drafts) én de **cockpit-KPI's overtelden open plekken** sinds PLANBORD-3. Alle 3 sites (autofillWeek, copyLastWeek, planner-intel confirmedExpr) omgebouwd naar grouped-query+Map (`liveCounts()`-helper); codebase-sweep bevestigt geen andere sites. | ✅ SHIPPED (type-check 0 · workers tsc 0 · build groen · **fix-E2E**: openSlotsBefore===raw-truth (2===2), run 2 = 0 gevuld (was 5), cockpit op échte counts). ⚠ Patroon-regel: nooit een gecorreleerde subquery in een drizzle-projectie. |
| **IA-fix: Rooster ≠ Planning ≠ Diensten (nav-verwarring opgelost)** (#152) | Owner-melding: "Planning voelt als een extra tab op Rooster". Oorzaak: "Planning" wees voor de OWNER naar de diensten-LIJST (`/admin/business/shifts`), terwijl de échte planning-werkbank (`/admin/planning`, 6 PR's werk) alleen in de planner-nav stond — owner kon 'm niet bereiken, en het woord "Planning" betekende per rol iets anders. Fix: **één consistent model voor beide rollen** — **Rooster** (`/business/roster`, read-only controletoren Dag/Week/Maand) · **Planning** (`/admin/planning`, de actie-werkbank) · **Diensten** (`/business/shifts`, de lijst). Owner-nav: "Planning"→werkbank + nieuwe "Diensten"→lijst. Plus: shifts-paginatitel "Shifts"→"Diensten", owner-cockpit-kaart "Planning→/shifts" → "Diensten·deze week→/admin/planning", roster-comment "future Planner" → bestaat nu, planning-eyebrow "Planner"→"Werkbank". Geen routes verwijderd; alleen labels/links/teksten. | ✅ SHIPPED (type-check 0 · build groen; owner heeft `planning.read` dus /admin/planning is bereikbaar). |
| **Chef-shell: phone-first PWA + bottom-nav + chat-uit** (#PR1, chef-wave 1/10) | Audit: chef-portal was desktop-shrunk (8 wrappende pills, 11px tekst), GEEN PWA, en — tegen owner-wens — stond er een LIVE chef-AI-chat (`AssistantWidget`→`/api/ai/portal/chat`). Nu: (1) **installeerbare PWA** — `src/app/manifest.ts` (scope `/chef`, standalone, burgundy theme), hand-rolled `public/sw.js` (app-shell cache + network-first `/chef/*` + werkende push/notificationclick-handlers voor PR-6), `PwaRegistrar` + `InstallPrompt` (beforeinstallprompt, dismiss-persist); icons via `scripts/gen-pwa-icons.mjs` (sharp ← logo.svg, any+maskable 192/512). (2) **phone-first `ChefNav`**: sticky bottom-tabbar <md (4 tabs + "Meer"-sheet, ≥44px targets, safe-area), top-pills md+. (3) **chef-chat UIT** achter `CHEF_AI_CHAT_ENABLED` (default off, `chefAiChatEnabled()`); chefs krijgen AI-hulp alleen indirect (komt in PR-3/4). (4) CSP `worker-src`/`manifest-src 'self'` (Report-Only blijft). Geen DB/worker. | ✅ SHIPPED (type-check 0 · build groen · **live serve-check**: /manifest.webmanifest = valid `application/manifest+json` scope `/chef`, /sw.js 200, /icons/icon-192.png 200 image/png). |
| **Chef-verdiensten: vooruitblik** (#PR2, chef-wave 2/10) | Chefs zagen alleen verleden-verdiensten; nu een **"Verwacht · komende N dagen"-kaart** op /chef/earnings: bevestigde shifts × tarief, per ISO-week + totaal. `getChefForecastEarnings` (nieuw `chef-forecast.ts`, búiten de gedeelde intel-lane): één grouped query (geen projectie-subquery — de neon-http-val), tarief-precedence placements→shifts→chefs.hourlyRateMin/Max (toont **range** als alleen de band bekend is), CONFIRMED-only (niet accepted → geen overbelofte). Eerlijke caveat: klanten kunnen annuleren + pauzes nog niet afgetrokken. Leeg → "nog niet ingeroosterd". Ships LIVE (read-only eigen data, geen flag). | ✅ SHIPPED (type-check 0 · build groen · SQL standaard, idem getChefPatterns). |
| **AI leest CV → profielsuggesties (owner-review)** (#PR3, chef-wave 3/10) | Owner-vraag: "AI moet CV's lezen om chef-profielen te verrijken (voor owner)". Dark achter `CV_AI_PROFILING_ENABLED`: tool **`chefs.enrich_from_cv`** (risk `self`) → `extractChefProfileFromCv` (R2 → unpdf → **redact** → OpenAI strict `json_schema`, injection-guard: CV = DATA, output enkel enum/array, géén PII) → `writeCvSuggestions` stageert in nieuwe **`profile_suggestions`**-tabel (migr 0050; partial-unique `(chef,field,hash) WHERE pending` = idempotente sweeps). Diff additief (segments/talen = union). Owner reviewt op de chef-pagina (`CvSuggestions`-kaart): **veilig veld** → direct op chefs; **vakniveau** (sensitive) → owner past direct toe (chef zou een `profile_change_request` indienen). Niets auto-toegepast, niemand gemaild. Eval: GOLDEN G-cv + SAFETY R-cvinj (CV-injectie mag geen destructieve tool openen). | ✅ SHIPPED (type-check 0 · smoke-tools 299/0 · smoke-safety **116/0** — fixte ook 3 sample-gaten van #7/#8 · build groen · migr 0050 op dev+prod · CI-eval groen). |
| **Chef vult eigen profiel aan (no-chat)** (#PR4, chef-wave 4/10) | Owner: "AI helpt de chef z'n profiel bouwen, zónder dat de chef met de AI praat." Nieuwe pagina **/chef/profile/compleet**: (1) **volledigheids-gaten** (getProfileCompleteness, alleen labels) + link naar editor; (2) **AI-voorstellen uit CV** (listPendingSuggestions) met Toevoegen/Negeren. Geen widget, geen ruwe CV-tekst. Accept routeert per field-class: veilig veld → direct op chefs (applyChefSafeField); vakniveau → `profile_change_request` voor kantoor. **Ownership-guard**: accept/dismiss kregen `expectChefId` (claim matcht alleen eigen suggesties). Banner op /chef/profile nudge't ("N voorstellen uit je CV"). | ✅ SHIPPED (type-check 0 · build groen; geen AI-paden → geen eval-gate). |
| **Nacht-sweep: CV → profielsuggesties** (#PR5, chef-wave 5/10) | Dark achter `CV_AI_PROFILING_ENABLED` (04:30 Amsterdam): `/api/cron/cv-profiling` + thin ticker `workers/cv-profiling.ts` + supervisor-JOB. Selecteert chefs met eigen-geüpload CV (zelfde selectie als ingestCvs, MAX_CHEFS=40/run), draait `extractChefProfileFromCv` → `writeCvSuggestions(null)`. **Idempotent**: writeCvSuggestions kreeg een dedup — velden die voor déze CV-versie (sourceHash) al accepted/dismissed zijn worden overgeslagen, dus een genegeerde suggestie komt niet elke nacht terug. Niets toegepast, niemand gemaild. | ✅ SHIPPED (type-check 0 · build groen · workers tsc 0; geen src/lib/ai-wijziging → geen eval-gate). |
| **Web Push: chef krijgt een seintje op de telefoon** (#PR6, chef-wave 6/10) | Dark achter `WEB_PUSH_ENABLED` + VAPID-keys. Nieuwe **`push_subscriptions`**-tabel (migr 0051, endpoint-unique, self-healing upsert) + domein (subscribe/unsubscribe/listActive/pruneDead). **`notifyUser()`**-wrapper: createNotification (de bel, altijd) + best-effort `enqueueIntegrationEvent('web_push', key notify.push:<id>)`. Delivery: `/api/cron/deliver-push` (web-push npm, VAPID, mute via prefs, prune 410/404) + thin ticker `workers/push-deliver.ts` (elke 2 min) + supervisor-JOB. **Opt-in UI** `PushOptIn` op /chef/meldingen (permissie → pushManager.subscribe → server action; iOS-niet-geïnstalleerd → add-to-homescreen-tekst). SW-push-handlers al klaar in PR-1. Bedraad: **nieuwe shift** (chef kreeg alleen mail → nu ook bel + push) + **uren afgekeurd**. Owner-actie: `npx web-push generate-vapid-keys` → Vercel/Railway env. | ✅ SHIPPED (type-check 0 · build groen · workers tsc 0 · migr 0051 dev+prod; geen src/lib/ai → geen eval-gate). |
| **WhatsApp-bridge: chef-meldingen ook via WhatsApp** (#PR7, chef-wave 7/10) | Dark achter `CHEF_WHATSAPP_ENABLED` + `whatsAppConfigured()`. `notifyUser()` kreeg een `whatsapp`-param → enqueue `provider:'whatsapp'` (key notify.whatsapp:<id>). De deliver-push-route drain't nu BEIDE kanalen onafhankelijk (per-flag gate): de WhatsApp-tak resolved userId→chefs.phone, mute-check `whatsapp:<type>`, stuurt via de bestaande `sendWhatsAppTemplate` (26 Meta-templates). Ticker draait als één van beide flags aan staat. Bedraad: **nieuwe shift** → `chef_nieuwe_dienst` (voornaam/klant/datum). Owner-actie: SENT_DM_API_KEY + Meta-template-goedkeuring vóór flip. | ✅ SHIPPED (type-check 0 · build groen · workers tsc 0; geen migr, geen src/lib/ai → geen eval-gate). |
| **Prikbord: schema + admin-compose** (#PR8, chef-wave 8/10) | Communaal bord. Migr 0052: **board_posts** (audience chefs/all, pinned, soft-delete) + **board_post_images** (R2, postId-FK cascade, r2Key-unique) + **board_reactions** (unique (post,user,emoji) → idempotente toggle in PR-9). RBAC **`board.write`** (CATALOG + OWNER_PLANNER_PERMS + GATE_MAP; parity board groen). Domein `board.ts` (createBoardPost/listAdminPosts/setPinned/softDeletePost + boardEnabled). **Admin-compose** /admin/business/board (tekst + pin + audience; lijst pinned-first met pin/verwijder; body = escaped text, géén dangerouslySetInnerHTML) + SidebarNav "Prikbord" (owner+planner). `boardImageKey` in r2.ts klaar voor PR-9. `BOARD_ENABLED` (default off) gate't de chef-feed + fan-out; owner-authoring werkt al. | ✅ SHIPPED (type-check 0 · parity board groen [77/1, de 1 = pre-existing assistant-flip] · build groen · migr 0052 dev+prod; geen src/lib/ai → geen eval-gate). |
| **Prikbord: chef-feed + reacties + afbeeldingen** (#PR9, chef-wave 9/10) | Chef-leeskant van het bord, achter `BOARD_ENABLED`. **/chef/board**: pinned-first feed (audience chefs/all), body als escaped text, afbeeldingen via presigned R2-GET, **emoji-reacties** (vast palet 👍❤️🔥👏😂) als pure `<form>`-toggle (geen client-JS) → `toggleReaction` (idempotent: SELECT→DELETE|INSERT onConflictDoNothing). Reactie-tellingen + "heb ik gereageerd" via **grouped query + Map** (geen projectie-subquery). ChefNav "Prikbord" in de Meer-sheet. Admin: per-post **`BoardImageUploader`** (presign → directe R2-PUT) + `requestBoardImageUpload`. (CSP Report-Only laat R2-img toe; enforce-flip voegt de host later toe.) | ✅ SHIPPED (type-check 0 · build groen; geen migr/AI → geen eval-gate). |
| **Prikbord: fan-out + push-handoff** (#PR10, chef-wave 10/10 — programma compleet) | Bij een nieuw bericht (achter `BOARD_ENABLED`): `announceBoardPost` → in-app bel-fan-out naar álle actieve chefs (`createNotificationsFanOut`, 1 bulk-insert) + ÉÉN `web_push` outbox-event (`board.new_post`, idempotent op postId, payload met userIds[]). De deliver-push-route handelt nu zowel single (`userId`, notify.push) als fan-out (`userIds[]`, board.new_post) af in dezelfde web_push-tak. | ✅ SHIPPED (type-check 0 · build groen; geen migr/AI → geen eval-gate). |
| **RBAC-flip: assistant-pagina van requireRole → requirePermission** (parity-fix) | `audit-permission-parity` meldde 1 failure: `/admin/assistant` gebruikte nog `requireRole("owner")` (laatste niet-geflipte gate). Nu: nieuwe owner-only perm **`assistant.use`** (CATALOG + OWNER_ONLY_PERMS + GATE_MAP `/admin/assistant`), pagina roept `requirePermission("assistant","use")`. Toegang identiek ({owner, super_admin} — assistant.use is owner-only + super_admin-bypass). | ✅ SHIPPED (parity **80/0**, "0 role-name gates remain" · type-check 0 · build groen; geen src/lib/ai → geen eval-gate). |
| **Open diensten: chef vraagt een dienst aan** (chef-flow, dark) | Owner-brief "rooster: ask for shifts" was de laatste ontbrekende flow — chefs konden alleen REAGEREN op voorstellen. Nu (dark `CHEF_OPEN_SHIFTS_ENABLED`, **express-interest** — planner cureert nog): nieuwe **`shift_interests`**-tabel (migr 0053, unique (shift,chef), withdrawnAt = actief). **/chef/open**: open toekomstige diensten (open headcount via grouped-count+Map, niet geblokkeerd via beschikbaarheid, niet al geplaatst) met "Ik heb interesse"/"intrekken"-toggle. Owner krijgt notificatie bij interesse. Planner ziet **"Chefs met interesse"** op de shift-detail met **Voorstel**-knop (hergebruikt `propose`/`proposePlacement`). ChefNav "Open diensten". Loop: chef meldt zich → owner genotificeerd + ziet 'm → stelt voor → chef bevestigt. | ✅ SHIPPED (type-check 0 · build groen · migr 0053 dev+prod; geen src/lib/ai → geen eval-gate). |
| **Chef-dashboard cockpit-strip** (chef-UX) | /chef home kreeg een compacte KPI-strip bovenaan (3 flex-tiles): **Verwacht** (forecast-totaal → /chef/earnings), **Profiel** (volledigheids-% → /chef/profile/compleet), **Open** (aantal open diensten → /chef/open, alleen als CHEF_OPEN_SHIFTS_ENABLED). Bindt de nieuwe features samen op de home: in één blik zie je wat je verwacht te verdienen, of je profiel af is, en welke diensten je kunt aanvragen. | ✅ SHIPPED (type-check 0 · build groen; geen migr/AI). |
| **Chef-dashboard: "Mijn cijfers"** (chef-UX) | /chef home kreeg een prestatie-sectie onder Komende: **Afgerond** (aantal completed placements) + **Beoordeling** (gemiddelde ★, alleen ≥5-regel via getChefSummaryForChef, anders "vanaf 5 beoordelingen"). Trots/motivatie-cijfers, los van de actie-cockpit bovenaan. | ✅ SHIPPED (type-check 0 · build groen; geen migr/AI). |
| **WhatsApp: owner kiest de ontvangers** (CHEF-15) | Owner-wens: de owner bepaalt wie WhatsApp krijgt; chefs kunnen het NIET uitzetten. Nieuw `chefs.whatsappEnabled` (migr 0054, default true, owner-controlled). Toggle op de admin chef-detail ("WhatsApp-meldingen aan/uit" + audit `chefs.whatsapp_toggled`). De deliver-push WhatsApp-tak haalt nu phone + whatsappEnabled op en slaat de chef over als de owner 'm heeft uitgezet (géén chef-opt-out). Globale gate blijft CHEF_WHATSAPP_ENABLED. | ✅ SHIPPED (type-check 0 · build groen · migr 0054 dev+prod; geen src/lib/ai → geen eval-gate). |
| **Prikbord: "Nieuw"-markering** (chef-UX) | Nieuw `chefs.boardSeenAt` (migr 0055). /chef/board leest de oude seenAt → markeert posts met createdAt > seenAt als **Nieuw** (emerald-badge), en bumpt daarna boardSeenAt=now voor het volgende bezoek. Eerste bezoek (null) → niets gemarkeerd (geen muur van badges). | ✅ SHIPPED (type-check 0 · build groen · migr 0055 dev+prod; geen AI). |
| **Chef-audit fixes: 3 bugs** (chef-side) | Final-audit-workflow (4 dims, adversarieel geverifieerd) vond 3 echte bugs: (1) **owner-cockpit "Profieldata"-CTA** wees naar `?filter=mist_data` maar de chefs-lijst leest `?data=incomplete` → owner landde op de volle lijst i.p.v. de onvolledige subset; gefixt (beide hrefs in admin/business/page.tsx). (2) **chef_visible placement-comments werden NOOIT aan de chef getoond** — admin "Zichtbaar voor chef"-berichten verdwenen stil; nu rendert /chef/shifts/[id] een "Berichten"-sectie via listVisibleComments(kind:"chef"). (3) **/chef/calendar (ICS-feed) was orphaned** (geen nav-link); toegevoegd als "Agenda-feed" in ChefNav. KPI-wiring zelf geverifieerd: géén projectie-subquery-traps in chef-KPI's (forecast/interests/intel grouped; money buckets count()). | ✅ SHIPPED (type-check 0 · build groen; geen migr/AI). |
| **AI-toegang tot de nieuwe chef-surfaces** (audit-fix, AI) | De final-audit (multi-agent) vond dat de AI de nét-gebouwde chef-data NIET kon zien. Nu 4 read-tools: **shifts.interested_chefs** (wie meldde zich aan voor open dienst X — listInterestedChefs), **chefs.pending_cv_suggestions** (openstaande CV-voorstellen — listPendingSuggestions), **chefs.reachability** (in-app/push/WhatsApp/e-mail als booleans + owner-toggle-status), **board.recent** (prikbord-berichten + reactietellingen — nieuwe listOwnerBoardActivity grouped). Nieuwe RBAC `board.read` (owner/planner). 9 GOLDEN-evalcases: de 4 nieuwe + de 5 zonder coverage (work_summary/feedback/profile_completeness/list_profile_changes/onboarding.request_missing). Alles read-only/AVG-veilig (labels/booleans, géén telefoon/e-mail-waarden). | ✅ SHIPPED (type-check 0 · parity 80/0 · smoke-tools 315/0 · smoke-safety 120/0 · build groen · CI-eval-gate op de PR). |
| **Klant-audit K1: portal/cockpit/AVG quick-wins** (klant-side) | Klant-side multi-agent audit (5 dims, adversarieel geverifieerd) → 20 gaps. K1 (quick): (1) owner-cockpit "voorgesteld, wacht op reactie"-CTA wees `?status=open` maar de shifts-lijst leest `?tab=open` → gefixt (zelfde bug-klasse als chef). (2) **AVG**: AI-directory (findChefs/findClients) selecteerde e-mail+telefoon in routing-resultaten → verwijderd (routing heeft enkel id/naam/stad nodig; contactdetails via chefs.work_summary/clients.health). (3) klant-hub: succes-flash na beoordeling (`ok=rated`). (4) dashboard "aanvragen wachten"-kaart kreeg CTA → /client/requests. (5) /client/calendar + /client/notifications waren orphaned → toegevoegd aan client-nav. Klant-KPI-wiring zelf geverifieerd: alles real-data, geen projectie-subquery-traps. | ✅ SHIPPED (type-check 0 · smoke-tools 315/0 · build groen · CI-eval op de PR [directory = AI]). |
